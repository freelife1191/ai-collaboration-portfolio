# 아키텍처 설계 문서
## [프로젝트 이름] - [프로젝트 설명]

**문서 버전**: [버전]
**작성일**: [작성일]
**작성자**: [작성자]

---

## 📋 목차

1. [시스템 아키텍처 개요](#시스템-아키텍처-개요)
2. [모노레포 아키텍처](#모노레포-아키텍처) 🏗️ **권장**
3. [컴포넌트 다이어그램](#컴포넌트-다이어그램)
4. [시퀀스 다이어그램](#시퀀스-다이어그램)
5. [데이터 흐름 다이어그램](#데이터-흐름-다이어그램)
6. [배포 아키텍처](#배포-아키텍처)
7. [인프라스트럭처](#인프라스트럭처)
8. [확장성 고려사항](#확장성-고려사항)
9. [보안 아키텍처](#보안-아키텍처)
10. [성능 및 최적화](#성능-및-최적화)

---

## 시스템 아키텍처 개요

**📐 다이어그램 작성 가이드:**

이 아키텍처 문서는 두 가지 다이어그램 방식을 지원합니다:
1. **ASCII 아트** (기본 권장) - 간단하고 빠른 표현, 텍스트 기반
2. **Mermaid** (선택 사용) - 복잡한 플로우, 시퀀스, 상태, ERD 등

**사용 시기:**
- **ASCII 아트**: 간단한 수직/수평 플로우, 레이어 구조, 체크리스트, 테이블
- **Mermaid**: 복잡한 다중 분기, API 호출 시퀀스, 상태 전환, DB 스키마, 컴포넌트 관계

**혼합 사용 가능**: 한 문서에서 ASCII 아트 + Mermaid를 함께 사용할 수 있습니다.

---

### High-Level 아키텍처 (ASCII)

```
┌────────────────────────────────────────────────────────────────────┐
│                         Client Layer                                │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐             │
│  │   Browser    │  │   Browser    │  │   Browser    │             │
│  │  (Desktop)   │  │   (Tablet)   │  │   (Mobile)   │             │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘             │
│         │                  │                  │                      │
│         └──────────────────┴──────────────────┘                      │
│                            │                                          │
│                         HTTPS                                         │
└────────────────────────────┼────────────────────────────────────────┘
                             │
                             ▼
┌────────────────────────────────────────────────────────────────────┐
│                    [CDN/Edge 네트워크]                              │
│                         ([설명])                                    │
└────────────────────────────┬───────────────────────────────────────┘
                             │
                             ▼
┌────────────────────────────────────────────────────────────────────┐
│                   [프레임워크] Application Layer                     │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │                    [라우터] (Pages)                          │   │
│  │  ┌────────────┐  ┌────────────┐  ┌────────────┐            │   │
│  │  │   [경로]   │  │   [경로]   │  │    [경로]  │            │   │
│  │  └────────────┘  └────────────┘  └────────────┘            │   │
│  └─────────────────────────────────────────────────────────────┘   │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │              [서버 컴포넌트]                                 │   │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐    │   │
│  │  │[컴포넌트]│  │[컴포넌트]│  │[컴포넌트]│  │[컴포넌트]│    │   │
│  │  │  [이름]  │  │  [이름]  │  │  [이름]  │  │  [이름]  │    │   │
│  │  └──────────┘  └──────────┘  └──────────┘  └──────────┘    │   │
│  └─────────────────────────────────────────────────────────────┘   │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │              [클라이언트 컴포넌트]                           │   │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐    │   │
│  │  │[컴포넌트]│  │[컴포넌트]│  │[컴포넌트]│  │[컴포넌트]│    │   │
│  │  │ [이름]   │  │ [이름]   │  │ [이름]   │  │ [이름]   │    │   │
│  │  └──────────┘  └──────────┘  └──────────┘  └──────────┘    │   │
│  └─────────────────────────────────────────────────────────────┘   │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │                  [서버 액션]                                 │   │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐    │   │
│  │  │ [기능]   │  │ [기능]   │  │ [기능]   │  │ [기능]   │    │   │
│  │  │  CRUD    │  │ [동작]   │  │ [동작]   │  │  Mgmt    │    │   │
│  │  └────┬─────┘  └────┬─────┘  └────┬─────┘  └────┬─────┘    │   │
│  └───────┼─────────────┼─────────────┼─────────────┼───────────┘   │
└──────────┼─────────────┼─────────────┼─────────────┼───────────────┘
           │             │             │             │
           ▼             ▼             ▼             ▼
┌──────────────────────────────────────────────────────────────┐
│                    External Services Layer                    │
│  ┌────────────┐  ┌────────────┐  ┌────────────┐             │
│  │            │  │            │  │            │             │
│  │ [서비스 1] │  │ [서비스 2] │  │ [서비스 3] │             │
│  │            │  │    API     │  │   Auth     │             │
│  │ [DB 시스템]│  │  [AI 모델] │  │  Service   │             │
│  │ [확장기능] │  │[임베딩 API]│  │            │             │
│  │  Storage   │  │            │  │            │             │
│  │            │  │            │  │            │             │
│  └────────────┘  └────────────┘  └────────────┘             │
│  ┌────────────┐                                              │
│  │ [서비스 4] │                                              │
│  │ [결제]     │                                              │
│  │  Billing   │                                              │
│  └────────────┘                                              │
└──────────────────────────────────────────────────────────────┘
```

### 아키텍처 패턴

**1. [패턴 이름 1]**
- **[컴포넌트 1]**: [기술 스택]
- **[컴포넌트 2]**: [기술 설명]
- **[컴포넌트 3]**: [렌더링 방식]

**2. [패턴 이름 2]**
- [특징 1]
- [특징 2]
- [특징 3]

**3. [패턴 이름 3]**
- [특징 1]
- [특징 2]
- [특징 3]

**4. [패턴 이름 4]**
- [특징 1]
- [특징 2]
- [특징 3]

---

## 모노레포 아키텍처

### 🏗️ 모노레포 개요

**모노레포(Monorepo)**는 여러 프로젝트/패키지를 하나의 저장소에서 관리하는 아키텍처 패턴입니다.

**장점:**
- ✅ **코드 공유 용이**: 공통 컴포넌트, 유틸리티, 타입 정의를 쉽게 공유
- ✅ **일관된 개발 환경**: 모든 프로젝트에서 동일한 도구 체인 사용
- ✅ **원자적 커밋**: 여러 패키지의 변경사항을 하나의 커밋으로 관리
- ✅ **의존성 관리 간소화**: 단일 package.json 또는 워크스페이스 관리
- ✅ **빌드 최적화**: 변경된 패키지만 선택적으로 빌드 (Turborepo 캐싱)

**단점:**
- ⚠️ 초기 설정 복잡도 증가
- ⚠️ 저장소 크기 증가
- ⚠️ CI/CD 파이프라인 복잡도 증가

---

### 📂 모노레포 디렉토리 구조

```
[모노레포 루트]/
├── apps/                                    # 애플리케이션들
│   ├── web/                                 # Next.js 웹 애플리케이션
│   │   ├── app/                            # App Router
│   │   ├── components/                     # 앱 전용 컴포넌트
│   │   ├── public/                         # 정적 파일
│   │   ├── next.config.js
│   │   ├── package.json
│   │   ├── tsconfig.json
│   │   └── tailwind.config.ts
│   │
│   ├── mobile/                              # React Native 모바일 앱 (선택)
│   │   ├── src/
│   │   ├── app.json
│   │   ├── package.json
│   │   └── tsconfig.json
│   │
│   └── admin/                               # 관리자 대시보드 (선택)
│       ├── app/
│       ├── components/
│       ├── package.json
│       └── tsconfig.json
│
├── packages/                                # 공유 패키지들
│   ├── ui/                                  # 공유 UI 컴포넌트 라이브러리
│   │   ├── src/
│   │   │   ├── components/
│   │   │   │   ├── Button/
│   │   │   │   │   ├── Button.tsx
│   │   │   │   │   ├── Button.test.tsx
│   │   │   │   │   └── index.ts
│   │   │   │   ├── Input/
│   │   │   │   ├── Card/
│   │   │   │   └── index.ts              # 모든 컴포넌트 export
│   │   │   └── index.ts
│   │   ├── package.json
│   │   ├── tsconfig.json
│   │   └── tailwind.config.ts
│   │
│   ├── utils/                               # 공유 유틸리티 함수
│   │   ├── src/
│   │   │   ├── formatters/                 # 포맷팅 함수
│   │   │   ├── validators/                 # 검증 함수
│   │   │   ├── helpers/                    # 헬퍼 함수
│   │   │   └── index.ts
│   │   ├── package.json
│   │   └── tsconfig.json
│   │
│   ├── types/                               # 공유 TypeScript 타입
│   │   ├── src/
│   │   │   ├── api.ts                      # API 응답 타입
│   │   │   ├── database.ts                 # DB 타입
│   │   │   ├── common.ts                   # 공통 타입
│   │   │   └── index.ts
│   │   ├── package.json
│   │   └── tsconfig.json
│   │
│   ├── config/                              # 공유 설정
│   │   ├── eslint-config/                  # ESLint 설정
│   │   │   ├── index.js
│   │   │   ├── next.js
│   │   │   ├── react.js
│   │   │   └── package.json
│   │   ├── typescript-config/              # TypeScript 설정
│   │   │   ├── base.json
│   │   │   ├── nextjs.json
│   │   │   ├── react.json
│   │   │   └── package.json
│   │   └── tailwind-config/                # Tailwind 설정
│   │       ├── base.js
│   │       └── package.json
│   │
│   ├── database/                            # 데이터베이스 클라이언트 및 스키마
│   │   ├── src/
│   │   │   ├── client.ts                   # Supabase/Prisma 클라이언트
│   │   │   ├── queries/                    # 재사용 가능한 쿼리
│   │   │   ├── migrations/                 # 마이그레이션
│   │   │   └── index.ts
│   │   ├── package.json
│   │   └── tsconfig.json
│   │
│   └── api-client/                          # 외부 API 클라이언트 (OpenAI 등)
│       ├── src/
│       │   ├── openai.ts
│       │   ├── stripe.ts
│       │   └── index.ts
│       ├── package.json
│       └── tsconfig.json
│
├── tooling/                                 # 개발 도구 (선택)
│   ├── scripts/                            # 공통 스크립트
│   │   ├── cleanup.sh
│   │   ├── setup.sh
│   │   └── deploy.sh
│   └── github/                             # GitHub Actions 워크플로우
│       └── workflows/
│           ├── ci.yml
│           ├── deploy.yml
│           └── test.yml
│
├── docs/                                    # 문서
│   ├── architecture/
│   ├── guides/
│   └── api/
│
├── .github/                                 # GitHub 설정
│   └── workflows/
│       ├── ci.yml
│       └── deploy-preview.yml
│
├── turbo.json                               # Turborepo 설정 (권장)
├── pnpm-workspace.yaml                      # pnpm 워크스페이스 설정
├── package.json                             # 루트 package.json
├── tsconfig.json                            # 루트 TypeScript 설정
├── .gitignore
├── .eslintrc.js
├── .prettierrc
└── README.md
```

---

### 🔗 패키지 간 의존성 관리

#### 패키지 참조 방식

**apps/web/package.json**:
```json
{
  "name": "@my-project/web",
  "version": "0.1.0",
  "dependencies": {
    "@my-project/ui": "workspace:*",
    "@my-project/utils": "workspace:*",
    "@my-project/types": "workspace:*",
    "@my-project/database": "workspace:*",
    "next": "14.2.0",
    "react": "^18.3.0"
  },
  "devDependencies": {
    "@my-project/eslint-config": "workspace:*",
    "@my-project/typescript-config": "workspace:*",
    "@my-project/tailwind-config": "workspace:*"
  }
}
```

**packages/ui/package.json**:
```json
{
  "name": "@my-project/ui",
  "version": "0.1.0",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "dependencies": {
    "@my-project/types": "workspace:*",
    "react": "^18.3.0"
  },
  "peerDependencies": {
    "react": "^18.0.0"
  }
}
```

---

### 🚀 Turborepo 설정 (권장)

**turbo.json**:
```json
{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": [".next/**", "dist/**", "build/**"],
      "cache": true
    },
    "test": {
      "dependsOn": ["^build"],
      "cache": true,
      "outputs": ["coverage/**"]
    },
    "lint": {
      "cache": true,
      "outputs": []
    },
    "type-check": {
      "dependsOn": ["^build"],
      "cache": true,
      "outputs": []
    },
    "dev": {
      "cache": false,
      "persistent": true
    },
    "clean": {
      "cache": false
    }
  },
  "globalDependencies": [
    ".env",
    "tsconfig.json",
    ".eslintrc.js"
  ],
  "globalEnv": [
    "NODE_ENV",
    "NEXT_PUBLIC_SUPABASE_URL",
    "NEXT_PUBLIC_SUPABASE_ANON_KEY"
  ]
}
```

---

### 📦 워크스페이스 설정

#### pnpm 워크스페이스 (권장)

**pnpm-workspace.yaml**:
```yaml
packages:
  - 'apps/*'
  - 'packages/*'
  - 'tooling/*'
```

**루트 package.json**:
```json
{
  "name": "@my-project/monorepo",
  "private": true,
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "test": "turbo run test",
    "test:unit": "turbo run test:unit",
    "test:integration": "turbo run test:integration",
    "test:e2e": "turbo run test:e2e",
    "lint": "turbo run lint",
    "type-check": "turbo run type-check",
    "clean": "turbo run clean && rm -rf node_modules",
    "format": "prettier --write \"**/*.{ts,tsx,md,json}\"",
    "prepare": "husky install"
  },
  "devDependencies": {
    "turbo": "^2.0.0",
    "prettier": "^3.3.0",
    "eslint": "^8.57.0",
    "typescript": "^5.5.0",
    "husky": "^9.0.0",
    "lint-staged": "^15.2.0"
  },
  "packageManager": "pnpm@9.1.0",
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  }
}
```

---

### 🏃 모노레포 빌드 파이프라인

```
┌─────────────────────────────────────────────────────────────────┐
│                    Turborepo Build Pipeline                      │
│                                                                  │
│  1️⃣ Dependency Graph 분석                                       │
│     ┌──────────┐         ┌──────────┐                          │
│     │   apps/  │ depends │ packages/│                          │
│     │   web    ├────────>│   ui     │                          │
│     └──────────┘         └────┬─────┘                          │
│                               │                                 │
│                               ▼ depends                         │
│                         ┌──────────┐                           │
│                         │ packages/│                           │
│                         │   types  │                           │
│                         └──────────┘                           │
│                                                                  │
│  2️⃣ 병렬 빌드 (캐싱 활용)                                       │
│     ┌──────────┐    ┌──────────┐    ┌──────────┐             │
│     │packages/ │    │packages/ │    │packages/ │             │
│     │  types   │    │  utils   │    │ database │             │
│     └────┬─────┘    └────┬─────┘    └────┬─────┘             │
│          │               │               │                     │
│          └───────────────┼───────────────┘                     │
│                          │                                     │
│                          ▼                                     │
│                   ┌──────────┐                                │
│                   │packages/ │                                │
│                   │   ui     │                                │
│                   └────┬─────┘                                │
│                        │                                       │
│                        ▼                                       │
│                 ┌──────────┐                                  │
│                 │  apps/   │                                  │
│                 │   web    │                                  │
│                 └──────────┘                                  │
│                                                                  │
│  3️⃣ 캐싱 레이어                                                 │
│     - Local Cache: .turbo/cache/                               │
│     - Remote Cache: Vercel Remote Cache (선택)                 │
│     - 변경되지 않은 패키지는 캐시에서 복원                       │
│                                                                  │
│  4️⃣ 선택적 빌드                                                 │
│     $ turbo run build --filter=@my-project/web                │
│     → web 앱과 의존하는 패키지만 빌드                           │
└─────────────────────────────────────────────────────────────────┘
```

---

### 🧪 모노레포 테스트 전략

```
┌─────────────────────────────────────────────────────────────────┐
│                      Test Organization                           │
│                                                                  │
│  📦 packages/ui/                                                 │
│     └── src/                                                    │
│         └── components/                                         │
│             └── Button/                                         │
│                 ├── Button.tsx                                  │
│                 ├── Button.test.tsx          ← Unit Test        │
│                 └── Button.stories.tsx       ← Storybook        │
│                                                                  │
│  📦 apps/web/                                                    │
│     ├── __tests__/                                              │
│     │   ├── unit/                           ← Unit Tests        │
│     │   ├── integration/                    ← Integration Tests │
│     │   └── e2e/                            ← E2E Tests         │
│     │       └── auth.spec.ts                                    │
│     └── app/                                                    │
│         └── (dashboard)/                                        │
│             └── page.test.tsx               ← Page Unit Test    │
│                                                                  │
│  🔄 테스트 실행 전략                                              │
│     - Unit Tests: 각 패키지별 독립 실행                          │
│     - Integration Tests: 패키지 조합 테스트                      │
│     - E2E Tests: apps/web에서만 실행                            │
│                                                                  │
│  📊 커버리지 집계                                                 │
│     $ turbo run test:coverage                                   │
│     → 모든 패키지의 커버리지를 통합 리포트                        │
└─────────────────────────────────────────────────────────────────┘
```

---

### 🔄 개발 워크플로우

**1. 로컬 개발**:
```bash
# 모든 앱을 동시에 dev 모드 실행
pnpm dev

# 특정 앱만 실행
pnpm dev --filter=@my-project/web

# UI 패키지 Storybook 실행
pnpm --filter=@my-project/ui storybook
```

**2. 빌드**:
```bash
# 전체 빌드 (의존성 순서대로 자동 빌드)
pnpm build

# 특정 앱만 빌드 (의존 패키지 자동 빌드)
pnpm build --filter=@my-project/web

# 병렬 빌드 (최대 성능)
pnpm build --parallel
```

**3. 테스트**:
```bash
# 모든 패키지 테스트
pnpm test

# 변경된 패키지만 테스트
pnpm test --filter=[origin/main]

# 특정 패키지 테스트
pnpm test --filter=@my-project/ui
```

**4. 린트 및 타입 체크**:
```bash
# 전체 린트
pnpm lint

# 전체 타입 체크
pnpm type-check

# 포맷팅
pnpm format
```

---

### 🚢 CI/CD 파이프라인 (모노레포)

**.github/workflows/ci.yml**:
```yaml
name: CI

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 2  # Turborepo 캐싱용

      - uses: pnpm/action-setup@v2
        with:
          version: 9.1.0

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Turbo Cache
        uses: actions/cache@v3
        with:
          path: .turbo
          key: ${{ runner.os }}-turbo-${{ github.sha }}
          restore-keys: |
            ${{ runner.os }}-turbo-

      - name: Type Check
        run: pnpm type-check

      - name: Lint
        run: pnpm lint

      - name: Test
        run: pnpm test:coverage

      - name: Build
        run: pnpm build

      - name: Upload Coverage
        uses: codecov/codecov-action@v3
        with:
          directory: ./coverage
```

---

### 📈 모노레포 이점 실현 예시

#### 1️⃣ 타입 안전성 공유

```typescript
// packages/types/src/api.ts
export interface User {
  id: string
  email: string
  name: string
  role: 'user' | 'admin' | 'pro'
}

export interface DocumentMetadata {
  id: string
  title: string
  userId: string
  createdAt: string
}
```

```typescript
// apps/web/app/api/users/route.ts
import type { User } from '@my-project/types'

export async function GET(): Promise<Response> {
  const users: User[] = await getUsers()
  return Response.json(users)
}
```

```typescript
// packages/database/src/queries/users.ts
import type { User } from '@my-project/types'
import { supabase } from '../client'

export async function getUsers(): Promise<User[]> {
  const { data } = await supabase.from('users').select('*')
  return data as User[]
}
```

**이점**: 타입 정의를 한 곳에서 관리하고 모든 패키지에서 동일한 타입 사용

---

#### 2️⃣ UI 컴포넌트 공유

```tsx
// packages/ui/src/components/Button/Button.tsx
import type { ButtonHTMLAttributes } from 'react'

interface ButtonProps extends ButtonHTMLAttributes<HTMLButtonElement> {
  variant?: 'primary' | 'secondary' | 'danger'
  size?: 'sm' | 'md' | 'lg'
}

export function Button({ variant = 'primary', size = 'md', ...props }: ButtonProps) {
  return <button className={cn(styles[variant], styles[size])} {...props} />
}
```

```tsx
// apps/web/app/dashboard/page.tsx
import { Button } from '@my-project/ui'

export default function DashboardPage() {
  return (
    <div>
      <Button variant="primary" size="lg">
        Create Document
      </Button>
    </div>
  )
}
```

```tsx
// apps/admin/app/users/page.tsx
import { Button } from '@my-project/ui'

export default function UsersPage() {
  return (
    <div>
      <Button variant="danger" size="sm">
        Delete User
      </Button>
    </div>
  )
}
```

**이점**: 웹, 관리자, 모바일 앱에서 동일한 디자인 시스템 사용

---

#### 3️⃣ 유틸리티 함수 공유

```typescript
// packages/utils/src/formatters/date.ts
export function formatDate(date: Date | string): string {
  const d = typeof date === 'string' ? new Date(date) : date
  return new Intl.DateTimeFormat('ko-KR', {
    year: 'numeric',
    month: 'long',
    day: 'numeric',
  }).format(d)
}
```

```typescript
// apps/web/components/DocumentCard.tsx
import { formatDate } from '@my-project/utils'

export function DocumentCard({ document }) {
  return (
    <div>
      <h3>{document.title}</h3>
      <p>{formatDate(document.createdAt)}</p>
    </div>
  )
}
```

**이점**: 모든 앱에서 동일한 날짜 포맷 사용, 중복 코드 제거

---

### ⚙️ 모노레포 모범 사례

#### ✅ DO (권장 사항)

1. **명확한 패키지 분리**
   - UI 컴포넌트는 `packages/ui`
   - 비즈니스 로직은 `packages/core` 또는 각 앱에
   - 타입 정의는 `packages/types`

2. **의존성 방향 일관성**
   ```
   apps/* → packages/* (O)
   packages/* → apps/* (X)
   ```

3. **버전 관리 전략**
   - 내부 패키지: `workspace:*` 사용
   - 외부 패키지: 루트에서 통합 관리

4. **테스트 격리**
   - 각 패키지는 독립적으로 테스트 가능해야 함
   - 통합 테스트는 apps/ 레벨에서 수행

5. **문서화**
   - 각 패키지에 README.md 작성
   - API 문서 자동 생성 (TypeDoc 등)

---

#### ❌ DON'T (피해야 할 사항)

1. **순환 의존성**
   ```
   packages/ui → packages/utils → packages/ui (X)
   ```

2. **apps 간 직접 참조**
   ```
   apps/web → apps/admin (X)
   ```
   → 공통 코드는 packages/로 추출

3. **패키지 내 하드코딩된 경로**
   ```typescript
   // 나쁜 예
   import { Button } from '../../../packages/ui/src/components/Button'

   // 좋은 예
   import { Button } from '@my-project/ui'
   ```

4. **너무 많은 패키지 분리**
   - 작은 프로젝트에서 과도한 분리는 오히려 복잡도 증가
   - 필요에 따라 점진적으로 분리

---

### 🎯 모노레포 적합성 판단

| 프로젝트 특성 | 모노레포 권장 | 멀티레포 권장 |
|--------------|--------------|--------------|
| 여러 앱 (웹, 모바일, 관리자) | ✅ | ❌ |
| 공통 UI/로직 많음 | ✅ | ❌ |
| 팀 규모 5명 이상 | ✅ | △ |
| 빠른 프로토타이핑 | △ | ✅ |
| 독립적인 배포 주기 | △ | ✅ |
| 단일 앱, 단순 구조 | ❌ | ✅ |

**결론**:
- **모노레포 권장**: 여러 앱을 운영하거나 공통 코드 공유가 많은 경우
- **멀티레포 권장**: 완전히 독립적인 프로젝트이거나 팀 규모가 작은 경우

---

## 컴포넌트 다이어그램

### Frontend 컴포넌트

```
┌─────────────────────────────────────────────────────────────┐
│                      [라우터] Structure                      │
│                                                              │
│  ┌──────────────────────────────────────────────────────┐   │
│  │  Root Layout ([파일명])                              │   │
│  │  ├─ [프로바이더 1]                                   │   │
│  │  ├─ [프로바이더 2]                                   │   │
│  │  └─ [컴포넌트]                                       │   │
│  └──────────────────────────────────────────────────────┘   │
│                           │                                  │
│         ┌─────────────────┴─────────────────┐               │
│         ▼                                   ▼               │
│  ┌─────────────┐                   ┌──────────────┐         │
│  │   [경로 1]  │                   │   [경로 2]   │         │
│  │ Route Group │                   │ Route Group  │         │
│  ├─────────────┤                   ├──────────────┤         │
│  │ [하위경로1] │                   │ [하위경로1]  │         │
│  │ [하위경로2] │                   │ [하위경로2]  │         │
│  └─────────────┘                   └──────┬───────┘         │
│                                            │                 │
│                               ┌────────────┴────────────┐   │
│                               │                         │   │
│                               ▼                         ▼   │
│                      ┌─────────────┐          ┌──────────┐  │
│                      │[동적 라우트]│          │   API    │  │
│                      │Dynamic Route│          │ Routes   │  │
│                      ├─────────────┤          ├──────────┤  │
│                      │ [페이지 1]  │          │ [경로 1] │  │
│                      │ [페이지 2]  │          │ [경로 2] │  │
│                      │ [페이지 3]  │          └──────────┘  │
│                      │ [페이지 4]  │                        │
│                      └─────────────┘                        │
└─────────────────────────────────────────────────────────────┘
```

### Backend 컴포넌트

```
┌──────────────────────────────────────────────────────────────┐
│                     Backend Architecture                      │
│                                                               │
│  ┌────────────────────────────────────────────────────────┐  │
│  │              [서버 액션] Layer                          │  │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐             │  │
│  │  │[모듈 1]  │  │[모듈 2]  │  │[모듈 3]  │             │  │
│  │  │Actions   │  │Actions   │  │Actions   │             │  │
│  │  └────┬─────┘  └────┬─────┘  └────┬─────┘             │  │
│  └───────┼─────────────┼─────────────┼────────────────────┘  │
│          │             │             │                        │
│          ▼             ▼             ▼                        │
│  ┌────────────────────────────────────────────────────────┐  │
│  │              Business Logic Layer                       │  │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐             │  │
│  │  │[매니저 1]│  │[프로세서]│  │[프로세서]│             │  │
│  │  │Manager   │  │Processor │  │Processor │             │  │
│  │  └────┬─────┘  └────┬─────┘  └────┬─────┘             │  │
│  └───────┼─────────────┼─────────────┼────────────────────┘  │
│          │             │             │                        │
│          ▼             ▼             ▼                        │
│  ┌────────────────────────────────────────────────────────┐  │
│  │              Data Access Layer                          │  │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐             │  │
│  │  │[클라이언트│  │[클라이언트│  │[서비스]  │             │  │
│  │  │  Client  │  │  Client  │  │ Service  │             │  │
│  │  └────┬─────┘  └────┬─────┘  └────┬─────┘             │  │
│  └───────┼─────────────┼─────────────┼────────────────────┘  │
│          │             │             │                        │
│          ▼             ▼             ▼                        │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐                   │
│  │[데이터베이│  │[외부 API]│  │[파일시스템│                   │
│  │  Database│  │API       │  │System    │                   │
│  └──────────┘  └──────────┘  └──────────┘                   │
└──────────────────────────────────────────────────────────────┘
```

---

## 시퀀스 다이어그램

### 1. [플로우 이름 1]

```
┌──────┐         ┌──────┐         ┌──────┐         ┌──────────┐
│Client│         │[FE]  │        │[인증] │         │[DB/API]  │
└───┬──┘         └───┬──┘         └───┬──┘         └────┬─────┘
    │                │                │                  │
    │ 1. [요청]      │                │                  │
    ├───────────────>│                │                  │
    │                │                │                  │
    │ 2. [UI 표시]   │                │                  │
    │<───────────────┤                │                  │
    │                │                │                  │
    │ 3. [제출]      │                │                  │
    │   [데이터]     │                │                  │
    ├───────────────>│                │                  │
    │                │                │                  │
    │                │ 4. [인증 처리] │                  │
    │                ├───────────────>│                  │
    │                │                │                  │
    │                │ 5. [토큰]      │                  │
    │                │<───────────────┤                  │
    │                │                │                  │
    │                │ 6. [쿠키 설정] │                  │
    │<───────────────┤                │                  │
    │                │                │                  │
    │ 7. [리다이렉트]│                │                  │
    │   [페이지]     │                │                  │
    │<───────────────┤                │                  │
    │                │                │                  │
    │ 8. [데이터]    │                │                  │
    │   [조회]       │                │                  │
    ├───────────────>│                │                  │
    │                │                │                  │
    │                │ 9. [쿼리]      │                  │
    │                │    [with Token]│                  │
    │                ├─────────────────────────────────>│
    │                │                │                  │
    │                │ 10. [권한 확인]│                  │
    │                │<─────────────────────────────────┤
    │                │                │                  │
    │                │ 11. [데이터]   │                  │
    │                │<─────────────────────────────────┤
    │                │                │                  │
    │ 12. [화면 표시]│                │                  │
    │<───────────────┤                │                  │
    │                │                │                  │
```

### 2. [플로우 이름 2]

```
┌──────┐    ┌──────┐    ┌────────┐    ┌────────┐    ┌──────┐
│Client│    │[FE]  │   │[저장소]│   │[외부API]│    │[파서]│
└───┬──┘    └───┬──┘    └───┬────┘    └───┬────┘    └───┬──┘
    │            │            │             │             │
    │ 1. [작업]  │            │             │             │
    │    [시작]  │            │             │             │
    ├───────────>│            │             │             │
    │            │            │             │             │
    │ 2. [검증]  │            │             │             │
    │    ([항목],│            │             │             │
    │     [항목])│            │             │             │
    │<───────────┤            │             │             │
    │            │            │             │             │
    │ 3. [데이터]│            │             │             │
    │    ([형식])│            │             │             │
    ├───────────>│            │             │             │
    │            │            │             │             │
    │            │ 4. [업로드]│             │             │
    │            │    [위치]  │             │             │
    │            ├───────────>│             │             │
    │            │            │             │             │
    │            │ 5. [URL]   │             │             │
    │            │<───────────┤             │             │
    │            │            │             │             │
    │            │ 6. [파싱]  │             │             │
    │            ├──────────────────────────────────────>│
    │            │            │             │             │
    │            │ 7. [추출]  │             │             │
    │            │    [데이터]│             │             │
    │            │<──────────────────────────────────────┤
    │            │            │             │             │
    │            │ 8. [처리] (병렬)                       │
    │            │            │             │             │
    │            │ 8a. [작업 1]             │             │
    │            ├──────────────────────────>│             │
    │            │            │             │             │
    │            │ 8b. [작업 2]             │             │
    │            ├──────────────────────────>│             │
    │            │            │             │             │
    │            │ 8c. [작업 3]             │             │
    │            ├──────────────────────────>│             │
    │            │            │             │             │
    │            │ 8d. [작업 4]             │             │
    │            ├──────────────────────────>│             │
    │            │            │             │             │
    │            │ 9. [결과]  │             │             │
    │            │<──────────────────────────┤             │
    │            │            │             │             │
    │            │ 10. [저장] │             │             │
    │            │     [메타] │             │             │
    │            ├───────────>│             │             │
    │            │            │             │             │
    │            │ 11. [저장] │             │             │
    │            │     [데이터│             │             │
    │            ├───────────>│             │             │
    │            │            │             │             │
    │            │ 12. [생성] │             │             │
    │            │     [완료] │             │             │
    │            │<───────────┤             │             │
    │            │            │             │             │
    │ 13. [성공] │            │             │             │
    │     +      │            │             │             │
    │   [데이터] │            │             │             │
    │<───────────┤            │             │             │
    │            │            │             │             │
```

### 3. [플로우 이름 3]

```
┌──────┐    ┌──────┐    ┌────────┐    ┌──────┐
│Client│    │[FE]  │   │[DB]    │   │[API] │
└───┬──┘    └───┬──┘    └───┬────┘    └───┬──┘
    │            │            │             │
    │ 1. [입력]  │            │             │
    │    [데이터]│            │             │
    ├───────────>│            │             │
    │            │            │             │
    │            │ 2. [생성]  │             │
    │            │    [벡터]  │             │
    │            ├──────────────────────────>│
    │            │            │             │
    │            │ 3. [결과]  │             │
    │            │    [벡터]  │             │
    │            │<──────────────────────────┤
    │            │            │             │
    │            │ 4. [벡터]  │             │
    │            │    [검색]  │             │
    │            ├───────────>│             │
    │            │            │             │
    │            │ 5. SQL:    │             │
    │            │    SELECT  │             │
    │            │    FROM    │             │
    │            │    [테이블]│             │
    │            │    ORDER BY│             │
    │            │    [기준]  │             │
    │            │    LIMIT   │             │
    │            │            │             │
    │            │ 6. [매칭]  │             │
    │            │    [데이터]│             │
    │            │    (with   │             │
    │            │    [점수]) │             │
    │            │<───────────┤             │
    │            │            │             │
    │            │ 7. [저장]  │             │
    │            │    [히스토리│             │
    │            ├───────────>│             │
    │            │            │             │
    │ 8. [표시]  │            │             │
    │    [결과]  │            │             │
    │    ([정렬] │            │             │
    │     [기준])│            │             │
    │<───────────┤            │             │
    │            │            │             │
```

---

## 데이터 흐름 다이어그램

### [프로세스 이름 1] 데이터 흐름

```
                      ┌──────────────┐
                      │    User      │
                      │   Browser    │
                      └──────┬───────┘
                             │
                    [데이터 선택] ([타입])
                             │
                             ▼
                      ┌──────────────┐
                      │  Validation  │
                      │  - [검증 1]  │
                      │  - [검증 2]  │
                      │  - [검증 3]  │
                      └──────┬───────┘
                             │
                         Valid ✓
                             │
                             ▼
              ┌──────────────────────────────┐
              │     [저장소]                  │
              │  [경로]/[파일명]              │
              └──────────────┬───────────────┘
                             │
                      [저장 완료]
                             │
                             ▼
              ┌──────────────────────────────┐
              │      [파서]                   │
              │  - [파서 1]                   │
              │  - [파서 2]                   │
              └──────────────┬───────────────┘
                             │
                     [데이터 추출]
                             │
              ┌──────────────┴──────────────┐
              │                             │
              ▼                             ▼
    ┌─────────────────┐          ┌─────────────────┐
    │  [외부 API 1]   │          │  [외부 API 2]   │
    │  - [작업 1]     │          │  - [작업 1]     │
    │  - [작업 2]     │          │    ([차원])     │
    └────────┬────────┘          └────────┬────────┘
             │                            │
       [결과 1]                      [결과 2]
             │                            │
             └─────────────┬──────────────┘
                           │
                           ▼
              ┌────────────────────────────┐
              │   [데이터베이스]            │
              │                            │
              │  [테이블 1]:                │
              │   - [컬럼 1]                │
              │   - [컬럼 2]                │
              │   - [컬럼 3]                │
              │   - [컬럼 4]                │
              │   - [컬럼 5]                │
              │   - [컬럼 6]                │
              │   - [컬럼 7]                │
              │                            │
              │  [테이블 2]:                │
              │   - [컬럼 1]                │
              │   - [컬럼 2]                │
              └────────────────────────────┘
```

### [프로세스 이름 2] 데이터 흐름

```
┌──────────────┐
│   [입력]     │
│   [데이터]   │
│ "[쿼리 예시]" │
└──────┬───────┘
       │
       ▼
┌──────────────────┐
│  [타입 선택]?    │
└──────┬───────────┘
       │
   ┌───┴────┐
   │        │
   ▼        ▼
[타입 1]  [타입 2]
[검색]    [검색]
   │        │
   │        │ ┌─────────────────┐
   │        └─┤  [외부 API]     │
   │          │  [변환 API]     │
   │          └─────────┬───────┘
   │                    │
   │            [변환된 데이터]
   │                    │
   │                    ▼
   │          ┌─────────────────┐
   │          │   [알고리즘]    │
   │          │   [유사도]      │
   │          │   [계산]        │
   │          └─────────┬───────┘
   │                    │
   ▼                    ▼
┌────────────────────────────┐
│  [데이터베이스]             │
│                            │
│  [타입 1] [검색]:          │
│  SELECT *                  │
│  FROM [테이블]             │
│  WHERE [조건]              │
│                            │
│  [타입 2] [검색]:          │
│  SELECT *                  │
│  FROM [테이블]             │
│  ORDER BY                  │
│  [기준]                    │
│  LIMIT [수량]              │
└────────────┬───────────────┘
             │
     [매칭된 데이터]
             │
             ▼
   ┌─────────────────┐
   │  [검색 결과]    │
   │  - [항목 1]     │
   │  - [항목 2]     │
   │  - [항목 3]     │
   └─────────────────┘
```

---

## 배포 아키텍처

### Production 배포 다이어그램

```
┌──────────────────────────────────────────────────────────────┐
│                        Global Users                           │
└───────────────────────┬──────────────────────────────────────┘
                        │
                        │ HTTPS
                        │
                        ▼
┌──────────────────────────────────────────────────────────────┐
│                   [CDN 네트워크]                              │
│                                                               │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐          │
│  │   Region    │  │   Region    │  │   Region    │          │
│  │    [지역1]  │  │    [지역2]  │  │    [지역3]  │          │
│  └─────────────┘  └─────────────┘  └─────────────┘          │
│                                                               │
│  - CDN ([에셋 타입])                                         │
│  - Edge Functions ([렌더링])                                │
│  - Automatic HTTPS                                           │
└───────────────────────┬──────────────────────────────────────┘
                        │
                        │
                        ▼
┌──────────────────────────────────────────────────────────────┐
│                [프레임워크] Application                       │
│                  (Serverless Functions)                       │
│                                                               │
│  ┌────────────────────────────────────────────────────────┐  │
│  │                 [서버 컴포넌트]                         │  │
│  │  - Pages ([렌더링])                                    │  │
│  │  - Layouts                                             │  │
│  └────────────────────────────────────────────────────────┘  │
│  ┌────────────────────────────────────────────────────────┐  │
│  │                 [서버 액션]                             │  │
│  │  - [기능] CRUD                                         │  │
│  │  - [기능] Upload                                       │  │
│  │  - [기능]                                              │  │
│  └────────────────────────────────────────────────────────┘  │
│  ┌────────────────────────────────────────────────────────┐  │
│  │                  API Routes                             │  │
│  │  - Webhooks ([서비스], [서비스])                       │  │
│  │  - Cron Jobs                                           │  │
│  └────────────────────────────────────────────────────────┘  │
└───────────┬──────────────────┬─────────────────┬────────────┘
            │                  │                 │
            │                  │                 │
            ▼                  ▼                 ▼
┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐
│   [서비스 1]    │  │   [서비스 2]    │  │   [서비스 3]    │
│   Cloud         │  │                 │  │   Auth          │
│                 │  │                 │  │                 │
│  [DB 시스템]    │  │  - [기능 1]     │  │  - [기능 1]     │
│  - [테이블 1]   │  │  - [기능 2]     │  │  - [기능 2]     │
│  - [테이블 2]   │  │                 │  │  - [기능 3]     │
│  - [확장기능]   │  │                 │  │                 │
│  - [보안기능]   │  │                 │  │                 │
│                 │  │                 │  │                 │
│  Storage        │  └─────────────────┘  └─────────────────┘
│  - [저장 타입]  │
│  - Public CDN   │  ┌─────────────────┐
│                 │  │  [서비스 4]     │
│  Region:        │  │   Billing       │
│  [지역 설명]    │  │                 │
└─────────────────┘  │  - [기능 1]     │
                     │  - [기능 2]     │
                     │  - [기능 3]     │
                     └─────────────────┘
```

### CI/CD 파이프라인

```
┌──────────────┐
│   Developer  │
│   Git Push   │
└──────┬───────┘
       │
       ▼
┌──────────────────────┐
│   [저장소]           │
│   - main branch      │
│   - feature branches │
└──────┬───────────────┘
       │
       │ Trigger
       │
       ▼
┌──────────────────────────────────────────┐
│         [CI/CD 도구]                     │
│                                          │
│  1. Lint ([린터])                       │
│     ├─ Check code quality               │
│     └─ Check formatting ([도구])        │
│                                          │
│  2. Type Check ([타입 시스템])          │
│     └─ [명령어]                         │
│                                          │
│  3. Test                                 │
│     ├─ Unit Tests ([도구])              │
│     ├─ Integration Tests                │
│     └─ E2E Tests ([도구])               │
│                                          │
│  4. Build                                │
│     └─ [빌드 명령어]                    │
│                                          │
│  5. Deploy Preview (PR)                 │
│     └─ [플랫폼] Preview Deployment      │
│                                          │
│  6. Deploy Production (main)            │
│     └─ [플랫폼] Production Deployment   │
└──────────────┬───────────────────────────┘
               │
               ▼
        ┌──────────────┐
        │   [배포환경] │
        │  Production  │
        └──────────────┘
```

---

## 인프라스트럭처

### [플랫폼] 설정

**프로젝트 설정**:
```json
{
  "name": "[프로젝트명]",
  "framework": "[프레임워크]",
  "buildCommand": "[빌드 명령어]",
  "devCommand": "[개발 명령어]",
  "installCommand": "[설치 명령어]",
  "outputDirectory": "[출력 디렉토리]",
  "regions": ["[지역1]", "[지역2]"],
  "functions": {
    "maxDuration": [시간]
  }
}
```

**환경 변수**:
```bash
# [서비스 1]
[ENV_VAR_1]=[값]
[ENV_VAR_2]=[값]
[ENV_VAR_3]=[값]

# [서비스 2]
[ENV_VAR_4]=[값]
[ENV_VAR_5]=[값]
[ENV_VAR_6]=[값]
[ENV_VAR_7]=[값]
[ENV_VAR_8]=[값]
[ENV_VAR_9]=[값]

# [서비스 3]
[ENV_VAR_10]=[값]

# [서비스 4]
[ENV_VAR_11]=[값]
[ENV_VAR_12]=[값]
[ENV_VAR_13]=[값]

# App
[ENV_VAR_14]=[값]
```

### [데이터베이스] 설정

**Database Configuration**:
```sql
-- Connection Pooling
ALTER SYSTEM SET max_connections = [값];
ALTER SYSTEM SET shared_buffers = '[값]';

-- Query Performance
ALTER SYSTEM SET effective_cache_size = '[값]';
ALTER SYSTEM SET random_page_cost = [값];

-- Extensions
CREATE EXTENSION IF NOT EXISTS "[확장기능1]";
CREATE EXTENSION IF NOT EXISTS "[확장기능2]";
CREATE EXTENSION IF NOT EXISTS "[확장기능3]";
```

**Storage Configuration**:
```json
{
  "buckets": [
    {
      "name": "[버킷명]",
      "public": false,
      "fileSizeLimit": [크기],
      "allowedMimeTypes": [
        "[MIME 타입1]",
        "[MIME 타입2]",
        "[MIME 타입3]",
        "[MIME 타입4]",
        "[MIME 타입5]"
      ]
    }
  ]
}
```

---

## 확장성 고려사항

### 수평 확장 (Horizontal Scaling)

**1. Application Layer**
- [플랫폼]은 자동으로 무한 확장
- 트래픽에 따라 [Function] 인스턴스 자동 증가
- 지역별 배포로 지연 시간 최소화

**2. Database Layer**
```sql
-- Read Replica 설정 ([플랜] 이상)
-- 읽기 부하 분산
SELECT * FROM [테이블] -- Read Replica로 자동 라우팅
```

**3. Storage Layer**
- [저장소]는 [기술] 기반으로 자동 확장
- CDN으로 정적 파일 캐싱

### 수직 확장 (Vertical Scaling)

**Database Upgrade Path**:
```
[플랜 1] ([용량])
    ↓
[플랜 2] ([용량])
    ↓
[플랜 3] ([용량])
    ↓
[플랜 4] (Custom)
```

### 캐싱 전략

**1. Client-Side Caching**
```[언어]
// [라이브러리]
const [클라이언트] = new [클래스]({
  defaultOptions: {
    queries: {
      staleTime: [시간], // [설명]
      cacheTime: [시간], // [설명]
    },
  },
})
```

**2. Server-Side Caching**
```[언어]
// [프레임워크] fetch cache
fetch(url, {
  next: {
    revalidate: [시간], // [설명]
    tags: ['[태그]'],
  },
})
```

**3. CDN Caching**
- [CDN 네트워크]
- [에셋 타입]: [기간]
- [페이지 타입]: [기간]

---

## 보안 아키텍처

### 인증 및 권한 체계

```
┌─────────────────────────────────────────┐
│           Authentication                 │
│              ([서비스])                  │
└──────────────┬──────────────────────────┘
               │
               │ [토큰 타입]
               │
               ▼
┌─────────────────────────────────────────┐
│         Authorization                    │
│      (Role-Based Access Control)        │
│                                          │
│  ┌────────┬─────────┬────────┬────────┐ │
│  │ [역할1]│ [역할2] │ [역할3]│ [역할4]│ │
│  └────────┴─────────┴────────┴────────┘ │
└──────────────┬──────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────┐
│      Row Level Security (RLS)           │
│         ([데이터베이스])                │
│                                          │
│  [권한 확인 쿼리]                        │
└─────────────────────────────────────────┘
```

### 데이터 보호 계층

```
┌──────────────────────────────────────────┐
│       Transport Layer ([프로토콜])       │
│       - All traffic HTTPS only           │
│       - Certificate auto-renewal         │
└──────────────┬───────────────────────────┘
               │
               ▼
┌──────────────────────────────────────────┐
│      Application Layer                   │
│      - Input validation ([도구])         │
│      - Output sanitization ([도구])      │
│      - CSRF protection (built-in)        │
└──────────────┬───────────────────────────┘
               │
               ▼
┌──────────────────────────────────────────┐
│       Database Layer                     │
│       - Row Level Security (RLS)         │
│       - Prepared statements              │
│       - Encryption at rest               │
└──────────────┬───────────────────────────┘
               │
               ▼
┌──────────────────────────────────────────┐
│       Storage Layer                      │
│       - Private buckets                  │
│       - Signed URLs                      │
│       - Encryption at rest ([기술])      │
└──────────────────────────────────────────┘
```

---

## 성능 및 최적화

### Performance Budget

| 지표 | 목표 | 측정 방법 |
|------|-----|----------|
| First Contentful Paint (FCP) | [목표] | [도구] |
| Largest Contentful Paint (LCP) | [목표] | [도구] |
| First Input Delay (FID) | [목표] | [도구] |
| Cumulative Layout Shift (CLS) | [목표] | [도구] |
| Time to Interactive (TTI) | [목표] | [도구] |
| Total Blocking Time (TBT) | [목표] | [도구] |

### 최적화 전략

**1. Code Splitting**
```[언어]
// Dynamic imports
const [컴포넌트] = dynamic(() => import('[경로]'), {
  ssr: false,
  loading: () => <[로딩 컴포넌트] />,
})

// Route-based splitting (automatic in [프레임워크])
```

**2. Image Optimization**
```[언어]
import [컴포넌트] from '[모듈]'

<[컴포넌트]
  src="[경로]"
  width={[너비]}
  height={[높이]}
  alt="[설명]"
  priority // [설명]
  quality={[품질]}
  placeholder="[타입]"
/>
```

**3. Database Query Optimization**
```sql
-- Index Strategy
CREATE INDEX [인덱스명]
  ON [테이블명]([컬럼명], [컬럼명] DESC);

-- Covering Index
CREATE INDEX [인덱스명]
  ON [테이블명]([컬럼명], [컬럼명] DESC)
  INCLUDE ([컬럼명], [컬럼명], [컬럼명]);

-- Partial Index
CREATE INDEX [인덱스명]
  ON [테이블명]([컬럼명], [컬럼명] DESC)
  WHERE [조건];
```

**4. API Response Optimization**
```[언어]
// Pagination
export async function [함수명](
  [파라미터]: string,
  { limit = [기본값], offset = [기본값] }
) {
  const { data, count } = await [클라이언트]
    .from('[테이블명]')
    .select('*', { count: 'exact' })
    .eq('[컬럼명]', [파라미터])
    .order('[컬럼명]', { ascending: false })
    .range(offset, offset + limit - 1)

  return { [항목]: data, total: count }
}
```

---

## 모니터링 및 로깅

### Monitoring Stack

```
┌─────────────────────────────────────────┐
│          Application Layer               │
│     ([프레임워크] + [분석 도구])        │
└──────────────┬──────────────────────────┘
               │
               ├─ [지표 타입 1]
               ├─ [지표 타입 2]
               └─ [지표 타입 3]
               │
               ▼
┌─────────────────────────────────────────┐
│         Monitoring Services              │
│                                          │
│  ┌──────────┐  ┌──────────┐  ┌────────┐│
│  │ [서비스1]│  │ [서비스2]│  │[서비스3││
│  │  [기능]  │  │ [기능]   │  │ [기능] ││
│  └──────────┘  └──────────┘  └────────┘│
└─────────────────────────────────────────┘
```

**로깅 예시**:
```[언어]
// Error Tracking
import * as [라이브러리] from '[패키지]'

[라이브러리].captureException(error, {
  tags: {
    component: '[컴포넌트명]',
    action: '[액션명]',
  },
  extra: {
    [변수1],
    [변수2],
  },
})

// Performance Tracking
[라이브러리].startTransaction({
  name: '[작업 이름]',
  op: '[작업 타입]',
})

// Custom Events
[라이브러리].capture('[이벤트명]', {
  [파라미터1]: [값],
  [파라미터2]: [값],
  [파라미터3]: [값],
})
```

---

이 아키텍처는 확장 가능하고 안전하며 성능이 뛰어난 [플랫폼 타입] 플랫폼을 구축하기 위한 기반을 제공합니다.
