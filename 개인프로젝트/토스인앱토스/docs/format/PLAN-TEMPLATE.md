# 프로젝트 구현 계획 및 로드맵
## [프로젝트 이름] - [프로젝트 설명]

> **프로젝트 기간**: [기간] (주당 [시간]시간)
> **목표**: [목표 설명]
> **최종 목표**: [최종 목표 설명]

---

## ⚠️ 개발 시작 전 필수 사항

### 🔧 환경 설정 완료 필수!

**이 PLAN.md를 시작하기 전에 반드시 완료해야 할 사항:**

📌 **[ENV-SETUP.md](./ENV-SETUP.md) 전체 완료 필수!**

**환경 설정 체크리스트:**
- [ ] Phase 0: 사전 준비 완료 (필요한 서비스 목록 확인)
- [ ] Phase 1: 로컬 개발 도구 설치 완료 (Node.js, Git, Docker)
- [ ] Phase 2: 서비스 계정 생성 완료 ([BaaS], [Auth], [AI] 등)
- [ ] Phase 3: API 키 수집 완료 (안전하게 `.env.local`에 저장)
- [ ] Phase 4: 데이터베이스 설정 완료 (연결 테스트 성공)
- [ ] Phase 5: MCP 서버 설치 완료 (Claude Desktop 연동)
- [ ] Phase 6: 환경 변수 설정 완료 (`.env`, `.env.local` 생성)
- [ ] Phase 7: 환경 검증 완료 (전체 통합 테스트 성공)

**완료 기준**: ✅ 위 체크리스트 모두 완료 후 이 PLAN.md 시작

**⚠️ 중요**: 환경 설정 없이 개발을 시작하면 중간에 막히거나 설정 문제로 시간을 낭비하게 됩니다!

---

## 📋 목차

0. [⚠️ 개발 시작 전 필수 사항](#개발-시작-전-필수-사항) ⭐
1. [프로젝트 개요](#프로젝트-개요)
2. [전체 일정 요약](#전체-일정-요약)
3. [Sprint 계획](#sprint-계획)
4. [Week 1: [주제]](#week-1-주제)
5. [Week 2: [주제]](#week-2-주제)
6. [Week 3: [주제]](#week-3-주제)
7. [마일스톤 및 체크포인트](#마일스톤-및-체크포인트)
8. [리스크 관리](#리스크-관리)
9. [성공 기준](#성공-기준)
10. [환경 변수 요약](#환경-변수-요약) ⭐

---

## 프로젝트 개요

### 핵심 목표
- [핵심 목표 1]
- [핵심 목표 2]
- [핵심 목표 3]
- [핵심 목표 4]

### 기술 스택
- **Frontend**: [프론트엔드 기술 스택]
- **Backend**: [백엔드 기술 스택]
- **Auth**: [인증 시스템]
- **AI**: [AI 서비스]
- **Database**: [데이터베이스]
- **Storage**: [스토리지]
- **Deployment**: [배포 플랫폼]

---

## 전체 일정 요약

```
Timeline ([N]주):
┌─────────────┬─────────────┬─────────────┐
│   Week 1    │   Week 2    │   Week 3    │
│  [주제]     │  [주제]     │  [주제]     │
├─────────────┼─────────────┼─────────────┤
│ - [작업1]   │ - [작업1]   │ - [작업1]   │
│ - [작업2]   │ - [작업2]   │ - [작업2]   │
│ - [작업3]   │ - [작업3]   │ - [작업3]   │
│             │             │             │
└─────────────┴─────────────┴─────────────┘
```

### 주요 마일스톤

| Week | Milestone | 완료 기준 |
|------|-----------|----------|
| Week 1 | [마일스톤 1] | [완료 기준] |
| Week 2 | [마일스톤 2] | [완료 기준] |
| Week 3 | [마일스톤 3] | [완료 기준] |

---

## Sprint 계획

### Sprint 1 (Week 1-2): [Sprint 이름]
**목표**: [Sprint 목표]

**Sprint Goal**:
- [목표 1]
- [목표 2]
- [목표 3]

**Sprint Backlog**:
1. [백로그 항목 1]
2. [백로그 항목 2]
3. [백로그 항목 3]
4. [백로그 항목 4]
5. [백로그 항목 5]

### Sprint 2 (Week 3): [Sprint 이름]
**목표**: [Sprint 목표]

**Sprint Goal**:
- [목표 1]
- [목표 2]
- [목표 3]

**Sprint Backlog**:
1. [백로그 항목 1]
2. [백로그 항목 2]
3. [백로그 항목 3]

---

## Week 1: [주제]

**목표**: [주간 목표]
**예상 시간**: [N-M]시간

---

### Day 1: [작업 주제] ([N-M]시간)

#### Morning ([N-M]시간)
**Task 1.1: [작업 이름]**
```bash
# [작업 설명]
[명령어 1]
[명령어 2]

cd [디렉토리]
```

**Task 1.2: [작업 이름]**
```bash
# [라이브러리 카테고리]
[패키지 관리자] add [패키지1] [패키지2]
[패키지 관리자] add [패키지3] [패키지4]

# [라이브러리 카테고리]
[패키지 관리자] add [패키지5] [패키지6]

# Dev Tools
[패키지 관리자] add -D [dev 패키지들]
```

**체크리스트**:
- [ ] [체크 항목 1]
- [ ] [체크 항목 2]
- [ ] [체크 항목 3]
- [ ] [체크 항목 4]
- [ ] [체크 항목 5]

#### Afternoon ([N-M]시간)
**Task 1.3: [작업 이름]**
```bash
# [작업 설명]
[명령어 1]

# [설명]
[명령어 2]
[명령어 3]
[명령어 4]
```

**Task 1.4: [작업 이름]**
```
[디렉토리]/
├── [폴더1]/
│   ├── [하위폴더1]/
│   ├── [하위폴더2]/
│   └── [하위폴더3]/
├── [폴더2]/
│   ├── [하위폴더1]/
│   ├── [하위폴더2]/
│   └── [하위폴더3]/
└── [폴더3]/
```

**체크리스트**:
- [ ] [체크 항목 1]
- [ ] [체크 항목 2]
- [ ] [체크 항목 3]

**Testing Checkpoint**:
- [ ] [테스트 항목 1]
- [ ] [테스트 항목 2]

---

### Day 2: [작업 주제] ([N-M]시간)

#### Morning ([N-M]시간)
**Task 2.1: [작업 이름]**

1. [단계 1 설명]
2. [단계 2 설명]
   - [세부 항목 1]: [값]
   - [세부 항목 2]: [값]
   - [세부 항목 3]: [값]
3. [단계 3 설명]

**Task 2.2: [작업 이름]**
```bash
[패키지 관리자] add [패키지명]
```

```typescript
// [파일 경로]
import { [import1] } from '[패키지]'

export function [함수명]() {
  return [구현](
    process.env.[ENV_VAR_1]!,
    process.env.[ENV_VAR_2]!
  )
}

// [파일 경로]
import { [import1], type [타입] } from '[패키지]'
import { [import2] } from '[모듈]'

export function [함수명]() {
  const [변수명] = [함수]()

  return [구현](
    process.env.[ENV_VAR_1]!,
    process.env.[ENV_VAR_2]!,
    {
      [옵션]: {
        [메서드](name: string) {
          return [변수명].get(name)?.value
        },
      },
    }
  )
}
```

**Task 2.3: [작업 이름]**
```bash
# [파일명]
[ENV_VAR_1]=[값]
[ENV_VAR_2]=[값]
[ENV_VAR_3]=[값]
```

**체크리스트**:
- [ ] [체크 항목 1]
- [ ] [체크 항목 2]
- [ ] [체크 항목 3]

#### Afternoon ([N-M]시간)
**Task 2.4: [작업 이름]**

1. [단계 1 설명]
2. [단계 2 설명]
   - [세부 항목 1]: [값]
   - [세부 항목 2]: [값]
3. [단계 3 설명]

```bash
[패키지 관리자] add [패키지명]
```

**Task 2.5: [작업 이름]**
```typescript
// [파일 경로]
import { [컴포넌트] } from '[패키지]'

export default function [컴포넌트명]({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <[컴포넌트]>
      <html lang="[언어]">
        <body>{children}</body>
      </html>
    </[컴포넌트]>
  )
}

// [파일명]
import { [함수] } from "[패키지]";

export default [함수]({
  publicRoutes: ["[경로1]", "[경로2]", "[경로3]"],
});

export const config = {
  matcher: ["[패턴1]", "[패턴2]", "[패턴3]"],
};
```

**Task 2.6: [작업 이름]**
```typescript
// [파일 경로]
import { [컴포넌트] } from "[패키지]";

export default function Page() {
  return <[컴포넌트] />;
}

// [파일 경로]
import { [컴포넌트] } from "[패키지]";

export default function Page() {
  return <[컴포넌트] />;
}
```

**체크리스트**:
- [ ] [체크 항목 1]
- [ ] [체크 항목 2]
- [ ] [체크 항목 3]
- [ ] [체크 항목 4]

**Testing Checkpoint**:
- [ ] [테스트 항목 1]
- [ ] [테스트 항목 2]
- [ ] [테스트 항목 3]

---

### Day 3: [작업 주제] ([N-M]시간)

#### Morning ([N]시간)
**Task 3.1: [작업 이름]**

[작업 설명]:

```sql
-- [설명]
CREATE EXTENSION IF NOT EXISTS "[extension명]";

-- 1. [테이블 이름]
CREATE TABLE [테이블명] (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  [필드명] TEXT NOT NULL,
  [필드명] TEXT UNIQUE NOT NULL,
  [필드명] TEXT NOT NULL, -- [설명]
  [필드명] TEXT NOT NULL DEFAULT '[값]', -- [옵션 설명]
  created_at TIMESTAMPTZ DEFAULT NOW(),
  updated_at TIMESTAMPTZ DEFAULT NOW()
);

-- Index
CREATE INDEX idx_[테이블명]_[필드명] ON [테이블명]([필드명]);
CREATE INDEX idx_[테이블명]_[필드명] ON [테이블명]([필드명]);

-- 2. [테이블 이름]
CREATE TABLE [테이블명] (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  [필드명] UUID REFERENCES [테이블명](id) ON DELETE CASCADE,
  [필드명] TEXT NOT NULL, -- [설명]
  [필드명] TEXT NOT NULL DEFAULT '[값]', -- [역할 설명]
  [필드명] TIMESTAMPTZ DEFAULT NOW(),
  UNIQUE([필드1], [필드2])
);

-- Index
CREATE INDEX idx_[테이블명]_[필드명] ON [테이블명]([필드명]);
CREATE INDEX idx_[테이블명]_[필드명] ON [테이블명]([필드명]);

-- 3. [테이블 이름]
CREATE TABLE [테이블명] (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  [필드명] UUID REFERENCES [테이블명](id) ON DELETE CASCADE,
  [필드명] TEXT NOT NULL,
  [필드명] TEXT NOT NULL, -- [파일 경로]
  [필드명] TEXT NOT NULL, -- [파일 타입]
  [필드명] INTEGER NOT NULL, -- [단위]
  [필드명] TEXT, -- [설명]
  [필드명] TEXT, -- [설명]
  [필드명] TEXT, -- [설명]
  [필드명] TEXT[], -- [설명]
  [필드명] TEXT NOT NULL, -- [설명]
  created_at TIMESTAMPTZ DEFAULT NOW(),
  updated_at TIMESTAMPTZ DEFAULT NOW()
);

-- Indexes
CREATE INDEX idx_[테이블명]_[필드명] ON [테이블명]([필드명]);
CREATE INDEX idx_[테이블명]_[필드명] ON [테이블명] USING GIN (to_tsvector('[언어]', [필드명]));
CREATE INDEX idx_[테이블명]_[필드명] ON [테이블명] USING GIN ([필드명]);
CREATE INDEX idx_[테이블명]_[필드명] ON [테이블명]([필드명]);
```

**Task 3.2: [작업 이름]**

```sql
-- Enable RLS
ALTER TABLE [테이블명1] ENABLE ROW LEVEL SECURITY;
ALTER TABLE [테이블명2] ENABLE ROW LEVEL SECURITY;
ALTER TABLE [테이블명3] ENABLE ROW LEVEL SECURITY;

-- [테이블 이름] Policies
CREATE POLICY "[정책 설명]"
  ON [테이블명] FOR SELECT
  USING (
    [필드명] = auth.jwt() ->> '[필드]' OR
    id IN (
      SELECT [필드명] FROM [테이블명]
      WHERE [필드명] = auth.jwt() ->> '[필드]'
    )
  );

CREATE POLICY "[정책 설명]"
  ON [테이블명] FOR INSERT
  WITH CHECK ([필드명] = auth.jwt() ->> '[필드]');

CREATE POLICY "[정책 설명]"
  ON [테이블명] FOR UPDATE
  USING ([필드명] = auth.jwt() ->> '[필드]');

CREATE POLICY "[정책 설명]"
  ON [테이블명] FOR DELETE
  USING ([필드명] = auth.jwt() ->> '[필드]');

-- [테이블 이름] Policies
CREATE POLICY "[정책 설명]"
  ON [테이블명] FOR SELECT
  USING (
    [필드명] = auth.jwt() ->> '[필드]' OR
    [필드명] IN (
      SELECT [필드명] FROM [테이블명]
      WHERE [필드명] = auth.jwt() ->> '[필드]' AND [필드명] IN ('[값1]', '[값2]')
    )
  );

CREATE POLICY "[정책 설명]"
  ON [테이블명] FOR INSERT
  WITH CHECK (
    [필드명] IN (
      SELECT [필드명] FROM [테이블명]
      WHERE [필드명] = auth.jwt() ->> '[필드]' AND [필드명] IN ('[값1]', '[값2]')
    )
  );
```

**체크리스트**:
- [ ] [체크 항목 1]
- [ ] [체크 항목 2]
- [ ] [체크 항목 3]
- [ ] [체크 항목 4]

#### Afternoon ([N-M]시간)
**Task 3.3: [작업 이름]**

```typescript
// [파일 경로]
export type Json =
  | string
  | number
  | boolean
  | null
  | { [key: string]: Json | undefined }
  | Json[]

export interface Database {
  public: {
    Tables: {
      [테이블명]: {
        Row: {
          id: string
          [필드명]: string
          [필드명]: string
          [필드명]: string
          [필드명]: '[타입1]' | '[타입2]' | '[타입3]'
          created_at: string
          updated_at: string
        }
        Insert: {
          id?: string
          [필드명]: string
          [필드명]: string
          [필드명]: string
          [필드명]?: '[타입1]' | '[타입2]' | '[타입3]'
          created_at?: string
          updated_at?: string
        }
        Update: {
          id?: string
          [필드명]?: string
          [필드명]?: string
          [필드명]?: string
          [필드명]?: '[타입1]' | '[타입2]' | '[타입3]'
          created_at?: string
          updated_at?: string
        }
      }
    }
  }
}
```

**Task 3.4: [작업 이름]**

```typescript
// [파일 경로]
"use server";

import { auth } from "[패키지]";
import { createClient } from "[경로]";
import { revalidatePath } from "next/cache";

export async function [함수명](formData: FormData) {
  const { userId } = await auth();
  if (!userId) throw new Error("Unauthorized");

  const [변수명] = formData.get("[필드명]") as string;
  const [변수명] = [변수명].toLowerCase().replace(/\s+/g, "-");

  const [클라이언트명] = createClient();

  // 1. [단계 설명]
  const { data: [변수명], error: [에러명] } = await [클라이언트명]
    .from("[테이블명]")
    .insert({
      [필드명]: [변수명],
      [필드명]: [변수명],
      [필드명]: userId,
      [필드명]: "[값]",
    })
    .select()
    .single();

  if ([에러명]) throw [에러명];

  // 2. [단계 설명]
  const { error: [에러명] } = await [클라이언트명]
    .from("[테이블명]")
    .insert({
      [필드명]: [변수명].id,
      [필드명]: userId,
      [필드명]: "[값]",
    });

  if ([에러명]) throw [에러명];

  revalidatePath("/");
  return [변수명];
}

export async function [함수명]() {
  const { userId } = await auth();
  if (!userId) throw new Error("Unauthorized");

  const [클라이언트명] = createClient();

  const { data, error } = await [클라이언트명]
    .from("[테이블명]")
    .select(`
      *,
      [조인테이블]!inner([필드])
    `)
    .eq("[조인테이블].[필드]", userId)
    .order("created_at", { ascending: false });

  if (error) throw error;
  return data;
}

export async function [함수명]([파라미터]: string) {
  const { userId } = await auth();
  if (!userId) throw new Error("Unauthorized");

  const [클라이언트명] = createClient();

  const { error } = await [클라이언트명]
    .from("[테이블명]")
    .delete()
    .eq("id", [파라미터])
    .eq("[필드명]", userId);

  if (error) throw error;

  revalidatePath("/");
}
```

**체크리스트**:
- [ ] [체크 항목 1]
- [ ] [체크 항목 2]
- [ ] [체크 항목 3]

**Testing Checkpoint**:
- [ ] [테스트 항목 1]
- [ ] [테스트 항목 2]
- [ ] [테스트 항목 3]

---

### Day 4: [작업 주제] ([N-M]시간)

#### Morning ([N]시간)
**Task 4.1: [작업 이름]**

```typescript
// [파일 경로]
"use client";

import Link from "next/link";
import { usePathname } from "next/navigation";
import { cn } from "[경로]";
import {
  [아이콘1],
  [아이콘2],
  [아이콘3],
} from "lucide-react";

const navItems = [
  { name: "[이름]", href: "", icon: [아이콘1] },
  { name: "[이름]", href: "[경로]", icon: [아이콘2] },
  { name: "[이름]", href: "[경로]", icon: [아이콘3] },
];

export function [컴포넌트명]({ [prop명] }: { [prop명]: string }) {
  const pathname = usePathname();

  return (
    <div className="[스타일]">
      <div className="[스타일]">
        <h2 className="[스타일]">[제목]</h2>
      </div>
      <nav className="[스타일]">
        {navItems.map((item) => {
          const href = `/${[prop명]}${item.href}`;
          const isActive = pathname === href;

          return (
            <Link
              key={item.name}
              href={href}
              className={cn(
                "[기본 스타일]",
                isActive
                  ? "[활성 스타일]"
                  : "[비활성 스타일]"
              )}
            >
              <item.icon className="[스타일]" />
              {item.name}
            </Link>
          );
        })}
      </nav>
    </div>
  );
}

// [파일 경로]
import { [컴포넌트] } from "[패키지]";

export function [컴포넌트명]() {
  return (
    <header className="[스타일]">
      <div className="[스타일]">
        <h1 className="[스타일]">[제목]</h1>
      </div>
      <[컴포넌트] afterSignOutUrl="/" />
    </header>
  );
}
```

**Task 4.2: [작업 이름]**

```typescript
// [파일 경로]
import { [컴포넌트1] } from "[경로]";
import { [컴포넌트2] } from "[경로]";

export default function [레이아웃명]({
  children,
  params,
}: {
  children: React.ReactNode;
  params: { [파라미터]: string };
}) {
  return (
    <div className="[스타일]">
      <[컴포넌트1] [prop명]={params.[파라미터]} />
      <div className="[스타일]">
        <[컴포넌트2] />
        <main className="[스타일]">{children}</main>
      </div>
    </div>
  );
}
```

**체크리스트**:
- [ ] [체크 항목 1]
- [ ] [체크 항목 2]
- [ ] [체크 항목 3]

#### Afternoon ([N-M]시간)
**Task 4.3: [작업 이름]**

```typescript
// [파일 경로]
import { [함수] } from "[경로]";
import { [컴포넌트1] } from "[경로]";
import { [컴포넌트2] } from "[경로]";

export default async function [페이지명]() {
  const [데이터명] = await [함수]();

  return (
    <div className="[스타일]">
      <div className="[스타일]">
        <div>
          <h1 className="[스타일]">[제목]</h1>
          <p className="[스타일]">
            [설명]
          </p>
        </div>
        <[컴포넌트2] />
      </div>
      <[컴포넌트1] [prop명]={[데이터명]} />
    </div>
  );
}

// [파일 경로]
"use client";

import { useState } from "react";
import { useRouter } from "next/navigation";
import { Button } from "[경로]";
import {
  Dialog,
  DialogContent,
  DialogHeader,
  DialogTitle,
  DialogTrigger,
} from "[경로]";
import { Input } from "[경로]";
import { Label } from "[경로]";
import { [함수] } from "[경로]";
import { [아이콘] } from "lucide-react";

export function [컴포넌트명]() {
  const [open, setOpen] = useState(false);
  const [loading, setLoading] = useState(false);
  const router = useRouter();

  async function handleSubmit(e: React.FormEvent<HTMLFormElement>) {
    e.preventDefault();
    setLoading(true);

    const formData = new FormData(e.currentTarget);

    try {
      const [결과] = await [함수](formData);
      setOpen(false);
      router.push(`/${[결과].[필드]}`);
    } catch (error) {
      console.error(error);
    } finally {
      setLoading(false);
    }
  }

  return (
    <Dialog open={open} onOpenChange={setOpen}>
      <DialogTrigger asChild>
        <Button>
          <[아이콘] className="[스타일]" />
          [버튼 텍스트]
        </Button>
      </DialogTrigger>
      <DialogContent>
        <DialogHeader>
          <DialogTitle>[제목]</DialogTitle>
        </DialogHeader>
        <form onSubmit={handleSubmit} className="[스타일]">
          <div>
            <Label htmlFor="[필드명]">[라벨]</Label>
            <Input
              id="[필드명]"
              name="[필드명]"
              placeholder="[placeholder]"
              required
            />
          </div>
          <Button type="submit" className="[스타일]" disabled={loading}>
            {loading ? "[로딩 텍스트]" : "[기본 텍스트]"}
          </Button>
        </form>
      </DialogContent>
    </Dialog>
  );
}
```

**체크리스트**:
- [ ] [체크 항목 1]
- [ ] [체크 항목 2]
- [ ] [체크 항목 3]

**Testing Checkpoint**:
- [ ] [테스트 항목 1]
- [ ] [테스트 항목 2]
- [ ] [테스트 항목 3]

---

### Day 5: [작업 주제] ([N-M]시간)

#### Morning ([N]시간)
**Task 5.1: [작업 이름]**

1. [단계 1 설명]
2. [단계 2 설명]: `[값]`
3. [단계 3 설명]:
   - [설정 1]: [값]
   - [설정 2]: [값]
   - [설정 3]: [설정값들]

**[정책 이름]:**
```sql
-- [설명]

-- [정책 설명]
CREATE POLICY "[정책명]"
ON [테이블명] FOR INSERT
TO authenticated
WITH CHECK ([조건]);

-- [정책 설명]
CREATE POLICY "[정책명]"
ON [테이블명] FOR SELECT
TO authenticated
USING (
  [조건1] AND
  ([조건2])[1] IN (
    SELECT [필드명] FROM [테이블명]
    WHERE [조건]
  )
);

-- [정책 설명]
CREATE POLICY "[정책명]"
ON [테이블명] FOR DELETE
TO authenticated
USING (
  [조건1] AND
  ([조건2])[1] IN (
    SELECT [필드명] FROM [테이블명]
    WHERE [조건]
  )
);
```

**Task 5.2: [작업 이름]**

```typescript
// [파일 경로]
"use server";

import { auth } from "[패키지]";
import { createClient } from "[경로]";
import { revalidatePath } from "next/cache";

const ALLOWED_FILE_TYPES = [
  "[MIME 타입1]",
  "[MIME 타입2]",
  "[MIME 타입3]",
];

const MAX_FILE_SIZE = [크기] * 1024 * 1024; // [설명]

export async function [함수명](
  [파라미터1]: string,
  formData: FormData
) {
  const { userId } = await auth();
  if (!userId) throw new Error("Unauthorized");

  const file = formData.get("[필드명]") as File;
  if (!file) throw new Error("No file provided");

  // Validate file type
  if (!ALLOWED_FILE_TYPES.includes(file.type)) {
    throw new Error("File type not supported");
  }

  // Validate file size
  if (file.size > MAX_FILE_SIZE) {
    throw new Error("[에러 메시지]");
  }

  const [클라이언트명] = createClient();

  // 1. [단계 설명]
  const [변수명] = `${Date.now()}-${file.name}`;
  const [변수명] = `${[파라미터1]}/${[변수명]}`;

  const { error: [에러명] } = await [클라이언트명].[메서드]
    .from("[버킷명]")
    .upload([변수명], file);

  if ([에러명]) throw [에러명];

  // 2. [단계 설명]
  const { data: [데이터명], error: [에러명] } = await [클라이언트명]
    .from("[테이블명]")
    .insert({
      [필드명]: [파라미터1],
      [필드명]: file.name,
      [필드명]: [변수명],
      [필드명]: file.type,
      [필드명]: file.size,
      [필드명]: userId,
    })
    .select()
    .single();

  if ([에러명]) throw [에러명];

  revalidatePath(`/${[파라미터1]}`);
  return [데이터명];
}

export async function [함수명]([파라미터]: string) {
  const { userId } = await auth();
  if (!userId) throw new Error("Unauthorized");

  const [클라이언트명] = createClient();

  const { data, error } = await [클라이언트명]
    .from("[테이블명]")
    .select("*")
    .eq("[필드명]", [파라미터])
    .order("created_at", { ascending: false });

  if (error) throw error;
  return data;
}

export async function [함수명]([파라미터]: string) {
  const { userId } = await auth();
  if (!userId) throw new Error("Unauthorized");

  const [클라이언트명] = createClient();

  // 1. [단계 설명]
  const { data: [데이터명], error: [에러명] } = await [클라이언트명]
    .from("[테이블명]")
    .select("[필드명]")
    .eq("id", [파라미터])
    .single();

  if ([에러명]) throw [에러명];

  // 2. [단계 설명]
  const { error: [에러명] } = await [클라이언트명].[메서드]
    .from("[버킷명]")
    .remove([[데이터명].[필드명]]);

  if ([에러명]) throw [에러명];

  // 3. [단계 설명]
  const { error: [에러명] } = await [클라이언트명]
    .from("[테이블명]")
    .delete()
    .eq("id", [파라미터]);

  if ([에러명]) throw [에러명];

  revalidatePath("/");
}
```

**체크리스트**:
- [ ] [체크 항목 1]
- [ ] [체크 항목 2]
- [ ] [체크 항목 3]
- [ ] [체크 항목 4]

#### Afternoon ([N-M]시간)
**Task 5.3: [작업 이름]**

```typescript
// [파일 경로]
"use client";

import { useState, useCallback } from "react";
import { useRouter } from "next/navigation";
import { useDropzone } from "react-dropzone";
import { Button } from "[경로]";
import { Card } from "[경로]";
import { Progress } from "[경로]";
import { [함수] } from "[경로]";
import { [아이콘1], [아이콘2], [아이콘3] } from "lucide-react";

interface [Props명] {
  [prop명]: string;
}

export function [컴포넌트명]({ [prop명] }: [Props명]) {
  const [uploading, setUploading] = useState(false);
  const [progress, setProgress] = useState(0);
  const [selectedFile, setSelectedFile] = useState<File | null>(null);
  const router = useRouter();

  const onDrop = useCallback((acceptedFiles: File[]) => {
    if (acceptedFiles.length > 0) {
      setSelectedFile(acceptedFiles[0]);
    }
  }, []);

  const { getRootProps, getInputProps, isDragActive } = useDropzone({
    onDrop,
    accept: {
      '[MIME 타입]': ['[확장자]'],
      '[MIME 타입]': ['[확장자]'],
    },
    maxSize: [크기] * 1024 * 1024, // [설명]
    multiple: false,
  });

  async function handleUpload() {
    if (!selectedFile) return;

    setUploading(true);
    setProgress(0);

    const formData = new FormData();
    formData.append("[필드명]", selectedFile);

    try {
      // [진행률 시뮬레이션]
      const progressInterval = setInterval(() => {
        setProgress((prev) => Math.min(prev + 10, 90));
      }, 200);

      await [함수]([prop명], formData);

      clearInterval(progressInterval);
      setProgress(100);

      // Reset
      setTimeout(() => {
        setSelectedFile(null);
        setProgress(0);
        setUploading(false);
        router.refresh();
      }, 500);
    } catch (error) {
      console.error(error);
      setUploading(false);
    }
  }

  return (
    <Card className="[스타일]">
      {!selectedFile ? (
        <div
          {...getRootProps()}
          className={`[기본 스타일] ${
            isDragActive
              ? "[활성 스타일]"
              : "[비활성 스타일]"
          }`}
        >
          <input {...getInputProps()} />
          <[아이콘1] className="[스타일]" />
          <p className="[스타일]">
            {isDragActive
              ? "[드래그 활성 메시지]"
              : "[기본 메시지]"}
          </p>
          <p className="[스타일]">
            [설명 텍스트]
          </p>
        </div>
      ) : (
        <div className="[스타일]">
          <div className="[스타일]">
            <div className="[스타일]">
              <[아이콘2] className="[스타일]" />
              <div>
                <p className="[스타일]">{selectedFile.name}</p>
                <p className="[스타일]">
                  {(selectedFile.size / 1024 / 1024).toFixed(2)} MB
                </p>
              </div>
            </div>
            <Button
              variant="ghost"
              size="icon"
              onClick={() => setSelectedFile(null)}
              disabled={uploading}
            >
              <[아이콘3] className="[스타일]" />
            </Button>
          </div>

          {uploading && (
            <div className="[스타일]">
              <Progress value={progress} />
              <p className="[스타일]">
                [진행 메시지] {progress}%
              </p>
            </div>
          )}

          <Button
            onClick={handleUpload}
            disabled={uploading}
            className="[스타일]"
          >
            {uploading ? "[업로드 중 텍스트]" : "[기본 텍스트]"}
          </Button>
        </div>
      )}
    </Card>
  );
}
```

**Task 5.4: [작업 이름]**

```typescript
// [파일 경로]
import { [함수] } from "[경로]";
import { [컴포넌트1] } from "[경로]";
import { [컴포넌트2] } from "[경로]";

interface PageProps {
  params: {
    [파라미터]: string;
  };
}

export default async function [페이지명]({ params }: PageProps) {
  // [설명]
  const [데이터명] = await [함수](params.[파라미터]);

  return (
    <div className="[스타일]">
      <div>
        <h1 className="[스타일]">[제목]</h1>
        <p className="[스타일]">
          [설명]
        </p>
      </div>

      <[컴포넌트1] [prop명]={params.[파라미터]} />

      <div>
        <h2 className="[스타일]">[제목]</h2>
        <[컴포넌트2] [prop명]={[데이터명]} />
      </div>
    </div>
  );
}
```

**Dependencies 추가:**
```bash
[패키지 관리자] add [패키지1] [패키지2]
```

**체크리스트**:
- [ ] [체크 항목 1]
- [ ] [체크 항목 2]
- [ ] [체크 항목 3]
- [ ] [체크 항목 4]
- [ ] [체크 항목 5]

**Testing Checkpoint**:
- [ ] [테스트 항목 1]
- [ ] [테스트 항목 2]
- [ ] [테스트 항목 3]
- [ ] [테스트 항목 4]
- [ ] [테스트 항목 5]

---

## Week 2: [주제]

**목표**: [주간 목표]
**예상 시간**: [N-M]시간

---

### Day 6-7: [작업 주제] ([N-M]시간)

#### Task 6.1: [작업 이름]
```bash
[패키지 관리자] add [패키지1] [패키지2] [패키지3]
[패키지 관리자] add -D [dev 패키지]
```

#### Task 6.2: [작업 이름]
```typescript
// [파일 경로]
import [라이브러리] from '[패키지]';

export async function [함수명](file: File): Promise<string> {
  const arrayBuffer = await file.arrayBuffer();
  const buffer = Buffer.from(arrayBuffer);

  try {
    const data = await [라이브러리](buffer);
    return data.text;
  } catch (error) {
    throw new Error(`[에러 메시지]: ${error}`);
  }
}
```

#### Task 6.3: [작업 이름]
```typescript
// [파일 경로]
import [라이브러리] from '[패키지]';

export async function [함수명](file: File): Promise<string> {
  const arrayBuffer = await file.arrayBuffer();
  const buffer = Buffer.from(arrayBuffer);

  try {
    const result = await [라이브러리].[메서드]({ buffer });
    return result.value;
  } catch (error) {
    throw new Error(`[에러 메시지]: ${error}`);
  }
}
```

#### Task 6.4: [작업 이름]
```typescript
// [파일 경로]
export async function [함수명](file: File): Promise<string> {
  return await file.text();
}

// [파일 경로]
import [라이브러리] from '[패키지]';
import { Readable } from 'stream';

export async function [함수명](file: File): Promise<string> {
  const text = await file.text();
  const rows: any[] = [];

  return new Promise((resolve, reject) => {
    const stream = Readable.from([text]);

    stream
      .pipe([라이브러리]())
      .on('data', (row) => rows.push(row))
      .on('end', () => {
        // [처리 설명]
        const result = rows
          .map((row) => Object.values(row).join(', '))
          .join('\n');
        resolve(result);
      })
      .on('error', reject);
  });
}
```

#### Task 6.5: [작업 이름]
```typescript
// [파일 경로]
import { [함수1] } from '[경로]';
import { [함수2] } from '[경로]';
import { [함수3] } from '[경로]';
import { [함수4] } from '[경로]';

export async function [함수명](file: File): Promise<string> {
  const mimeType = file.type;

  try {
    switch (mimeType) {
      case '[MIME 타입1]':
        return await [함수1](file);

      case '[MIME 타입2]':
        return await [함수2](file);

      case '[MIME 타입3]':
      case '[MIME 타입4]':
        return await [함수3](file);

      case '[MIME 타입5]':
        return await [함수4](file);

      default:
        throw new Error(`[에러 메시지]: ${mimeType}`);
    }
  } catch (error) {
    throw new Error(`[에러 메시지]: ${error}`);
  }
}
```

**체크리스트**:
- [ ] [체크 항목 1]
- [ ] [체크 항목 2]
- [ ] [체크 항목 3]
- [ ] [체크 항목 4]
- [ ] [체크 항목 5]
- [ ] [체크 항목 6]

---

### Day 8-9: [작업 주제] ([N-M]시간)

#### Task 7.1: [작업 이름]
```bash
[패키지 관리자] add [패키지명]
```

```typescript
// [파일 경로]
import [클래스] from '[패키지]';

if (!process.env.[ENV_VAR]) {
  throw new Error('[에러 메시지]');
}

export const [인스턴스명] = new [클래스]({
  apiKey: process.env.[ENV_VAR],
});
```

#### Task 7.2: [작업 이름]
```typescript
// [파일 경로]
import { [인스턴스명] } from '[경로]';

export async function [함수명](
  [파라미터1]: string,
  [파라미터2]: number = [기본값]
): Promise<string> {
  try {
    const response = await [인스턴스명].[메서드].[메서드]({
      model: '[모델명]',
      messages: [
        {
          role: 'system',
          content: `[시스템 프롬프트]
          [추가 지시사항] ${[파라미터2]} [단위].`,
        },
        {
          role: 'user',
          content: `[사용자 프롬프트]:\n\n${[파라미터1].slice(0, [길이])}`,
        },
      ],
      temperature: [온도],
      max_tokens: Math.floor([파라미터2] / [나누기값]),
    });

    return response.choices[0].message.content || '';
  } catch (error) {
    console.error('[에러 타입]:', error);
    throw new Error('[에러 메시지]');
  }
}
```

#### Task 7.3: [작업 이름]
```typescript
// [파일 경로]
import { [인스턴스명] } from '[경로]';

export async function [함수명](
  [파라미터1]: string,
  [파라미터2]: number = [기본값]
): Promise<string[]> {
  try {
    const response = await [인스턴스명].[메서드].[메서드]({
      model: '[모델명]',
      messages: [
        {
          role: 'system',
          content: `[시스템 프롬프트]
          [추가 지시사항] ${[파라미터2]} [단위].`,
        },
        {
          role: 'user',
          content: `[사용자 프롬프트]:\n\n${[파라미터1].slice(0, [길이])}`,
        },
      ],
      temperature: [온도],
      max_tokens: [토큰수],
    });

    const [변수명] = response.choices[0].message.content || '';
    return [변수명]
      .split(',')
      .map((kw) => kw.trim())
      .filter((kw) => kw.length > 0)
      .slice(0, [파라미터2]);
  } catch (error) {
    console.error('[에러 타입]:', error);
    throw new Error('[에러 메시지]');
  }
}
```

#### Task 7.4: [작업 이름]
```typescript
// [파일 경로]
import { [인스턴스명] } from '[경로]';

export async function [함수명](text: string): Promise<number[]> {
  try {
    const response = await [인스턴스명].[메서드].create({
      model: '[모델명]',
      input: text.slice(0, [길이]), // [설명]
    });

    return response.data[0].embedding;
  } catch (error) {
    console.error('[에러 타입]:', error);
    throw new Error('[에러 메시지]');
  }
}
```

#### Task 7.5: [작업 이름]
```typescript
// [파일 경로]
import { [함수1] } from '[경로]';
import { [함수2] } from '[경로]';
import { [함수3] } from '[경로]';

export interface [인터페이스명] {
  [필드명1]: string;
  [필드명2]: string;
  [필드명3]: string[];
  [필드명4]: number[];
}

export async function [함수명](
  [파라미터]: string
): Promise<[인터페이스명]> {
  try {
    // [설명]
    const [[변수1], [변수2], [변수3], [변수4]] = await Promise.all([
      [함수1]([파라미터], [값1]),
      [함수1]([파라미터], [값2]),
      [함수2]([파라미터], [값3]),
      [함수3]([파라미터]),
    ]);

    return {
      [필드명1]: [변수1],
      [필드명2]: [변수2],
      [필드명3]: [변수3],
      [필드명4]: [변수4],
    };
  } catch (error) {
    console.error('[에러 타입]:', error);
    throw error;
  }
}
```

**환경 변수 추가:**
```bash
# [파일명]
[ENV_VAR]=[값]
```

**체크리스트**:
- [ ] [체크 항목 1]
- [ ] [체크 항목 2]
- [ ] [체크 항목 3]
- [ ] [체크 항목 4]
- [ ] [체크 항목 5]
- [ ] [체크 항목 6]
- [ ] [체크 항목 7]

---

### Day 10: [작업 주제] ([N]시간)

#### Task 8.1: [작업 이름]

[작업 설명]:
```sql
-- Enable [extension]
CREATE EXTENSION IF NOT EXISTS [extension명];

-- [테이블 설명]
CREATE TABLE [테이블명] (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  [필드명] UUID REFERENCES [테이블명](id) ON DELETE CASCADE,
  [필드명] INTEGER NOT NULL DEFAULT 0,
  [필드명] TEXT NOT NULL,
  [필드명] VECTOR([차원]), -- [설명]
  created_at TIMESTAMPTZ DEFAULT NOW(),
  UNIQUE([필드1], [필드2])
);

-- [인덱스 설명]
CREATE INDEX ON [테이블명] USING ivfflat ([필드명] vector_cosine_ops)
WITH (lists = [값]);

-- RLS
ALTER TABLE [테이블명] ENABLE ROW LEVEL SECURITY;

CREATE POLICY "[정책 설명]"
  ON [테이블명] FOR SELECT
  USING (
    [필드명] IN (
      SELECT id FROM [테이블명]
      WHERE [필드명] IN (
        SELECT [필드명] FROM [테이블명]
        WHERE [필드명] = auth.jwt() ->> '[필드]'
      )
    )
  );
```

#### Task 8.2: [작업 이름]

```typescript
// [파일 경로] (업데이트)
"use server";

import { auth } from "[패키지]";
import { createClient } from "[경로]";
import { revalidatePath } from "next/cache";
import { [함수1] } from "[경로]";
import { [함수2] } from "[경로]";

export async function [함수명](
  [파라미터1]: string,
  formData: FormData
) {
  const { userId } = await auth();
  if (!userId) throw new Error("Unauthorized");

  const file = formData.get("[필드명]") as File;
  if (!file) throw new Error("No file provided");

  const [클라이언트명] = createClient();

  try {
    // 1. [단계 설명]
    const [변수명] = `${Date.now()}-${file.name}`;
    const [변수명] = `${[파라미터1]}/${[변수명]}`;

    const { error: [에러명] } = await [클라이언트명].[메서드]
      .from("[버킷명]")
      .upload([변수명], file);

    if ([에러명]) throw [에러명];

    // 2. [단계 설명]
    const [변수명] = await [함수1](file);

    // 3. [단계 설명]
    const [변수명] = await [함수2]([변수명]);

    // 4. [단계 설명]
    const { data: [데이터명], error: [에러명] } = await [클라이언트명]
      .from("[테이블명]")
      .insert({
        [필드명]: [파라미터1],
        [필드명]: file.name,
        [필드명]: [변수명],
        [필드명]: file.type,
        [필드명]: file.size,
        [필드명]: [변수명],
        [필드명]: [변수명].[필드1],
        [필드명]: [변수명].[필드2],
        [필드명]: [변수명].[필드3],
        [필드명]: userId,
      })
      .select()
      .single();

    if ([에러명]) throw [에러명];

    // 5. [단계 설명]
    const { error: [에러명] } = await [클라이언트명]
      .from("[테이블명]")
      .insert({
        [필드명]: [데이터명].id,
        [필드명]: 0,
        [필드명]: [변수명],
        [필드명]: [변수명].[필드],
      });

    if ([에러명]) throw [에러명];

    revalidatePath(`/${[파라미터1]}`);
    return [데이터명];
  } catch (error) {
    console.error("[에러 타입]:", error);
    throw error;
  }
}
```

**체크리스트**:
- [ ] [체크 항목 1]
- [ ] [체크 항목 2]
- [ ] [체크 항목 3]
- [ ] [체크 항목 4]
- [ ] [체크 항목 5]
- [ ] [체크 항목 6]

**Testing Checkpoint**:
- [ ] [테스트 항목 1]
- [ ] [테스트 항목 2]
- [ ] [테스트 항목 3]
- [ ] [테스트 항목 4]
- [ ] [테스트 항목 5]

**테스트 실행 및 검증**:
```bash
# 단위 테스트 실행
[패키지 관리자] test

# 커버리지 리포트 생성
[패키지 관리자] test:cov

# E2E 테스트 실행 (있는 경우)
[패키지 관리자] test:e2e
```

**📄 TEST-REPORT.md 업데이트**:
- [ ] Week/Day별 테스트 결과 섹션 추가
- [ ] 테스트 통과율 업데이트
- [ ] 커버리지 리포트 추가
- [ ] 테스트 실패 이력 기록 (실패 시)
- [ ] 검증 완료 날짜 기록

---

## Week 3: [주제]

**목표**: [주간 목표]
**예상 시간**: [N-M]시간

---

### Day 11-12: [작업 주제] ([N-M]시간)

#### Task 9.1: [작업 이름]

```sql
-- [작업 설명]
CREATE OR REPLACE FUNCTION [함수명](
  [파라미터1] VECTOR([차원]),
  [파라미터2] FLOAT,
  [파라미터3] INT,
  [파라미터4] UUID
)
RETURNS TABLE (
  [필드명1] UUID,
  [필드명2] TEXT,
  [필드명3] TEXT,
  [필드명4] TEXT,
  [필드명5] TEXT[],
  [필드명6] FLOAT,
  [필드명7] TIMESTAMPTZ
)
LANGUAGE plpgsql
AS $$
BEGIN
  RETURN QUERY
  SELECT
    [테이블1].[필드] AS [별칭1],
    [테이블1].[필드],
    [테이블2].[필드],
    [테이블1].[필드],
    [테이블1].[필드],
    1 - ([테이블2].[필드] <=> [파라미터1]) AS [별칭2],
    [테이블1].[필드]
  FROM [테이블명1] [별칭1]
  JOIN [테이블명2] [별칭2] ON [별칭2].[필드] = [별칭1].[필드]
  WHERE [별칭2].[필드] = [파라미터4]
    AND 1 - ([별칭1].[필드] <=> [파라미터1]) > [파라미터2]
  ORDER BY [별칭1].[필드] <=> [파라미터1]
  LIMIT [파라미터3];
END;
$$;
```

#### Task 9.2: [작업 이름]

```typescript
// [파일 경로]
"use server";

import { auth } from "[패키지]";
import { createClient } from "[경로]";
import { [함수] } from "[경로]";

export async function [함수명1](
  [파라미터1]: string,
  [파라미터2]: string,
  [파라미터3]: number = [기본값]
) {
  const { userId } = await auth();
  if (!userId) throw new Error("Unauthorized");

  const [클라이언트명] = createClient();

  try {
    // 1. [단계 설명]
    const [변수명] = await [함수]([파라미터2]);

    // 2. [단계 설명]
    const { data, error } = await [클라이언트명].rpc("[함수명]", {
      [인자1]: [변수명],
      [인자2]: [값],
      [인자3]: [파라미터3],
      [인자4]: [파라미터1],
    });

    if (error) throw error;

    // 3. [단계 설명]
    await [클라이언트명].from("[테이블명]").insert({
      [필드명]: [파라미터1],
      [필드명]: userId,
      [필드명]: [파라미터2],
      [필드명]: "[타입]",
    });

    return data;
  } catch (error) {
    console.error("[에러 타입]:", error);
    throw error;
  }
}

export async function [함수명2](
  [파라미터1]: string,
  [파라미터2]: string,
  [파라미터3]: number = [기본값]
) {
  const { userId } = await auth();
  if (!userId) throw new Error("Unauthorized");

  const [클라이언트명] = createClient();

  try {
    const { data, error } = await [클라이언트명]
      .from("[테이블명]")
      .select("*")
      .eq("[필드명]", [파라미터1])
      .or(`[필드명].ilike.%${[파라미터2]}%,[필드명].ilike.%${[파라미터2]}%`)
      .limit([파라미터3])
      .order("created_at", { ascending: false });

    if (error) throw error;

    // [단계 설명]
    await [클라이언트명].from("[테이블명]").insert({
      [필드명]: [파라미터1],
      [필드명]: userId,
      [필드명]: [파라미터2],
      [필드명]: "[타입]",
    });

    return data;
  } catch (error) {
    console.error("[에러 타입]:", error);
    throw error;
  }
}
```

#### Task 9.3: [작업 이름]

```typescript
// [파일 경로]
"use client";

import { useState } from "react";
import { Input } from "[경로]";
import { Button } from "[경로]";
import { Tabs, TabsContent, TabsList, TabsTrigger } from "[경로]";
import { [함수1], [함수2] } from "[경로]";
import { [아이콘] } from "lucide-react";

export default function [페이지명]({
  params,
}: {
  params: { [파라미터]: string };
}) {
  const [query, setQuery] = useState("");
  const [results, setResults] = useState<any[]>([]);
  const [loading, setLoading] = useState(false);
  const [searchType, setSearchType] = useState<"[타입1]" | "[타입2]">("[타입1]");

  async function handleSearch() {
    if (!query.trim()) return;

    setLoading(true);
    try {
      const data =
        searchType === "[타입1]"
          ? await [함수1](params.[파라미터], query)
          : await [함수2](params.[파라미터], query);

      setResults(data);
    } catch (error) {
      console.error(error);
    } finally {
      setLoading(false);
    }
  }

  return (
    <div className="[스타일]">
      <div>
        <h1 className="[스타일]">[제목]</h1>
        <p className="[스타일]">
          [설명]
        </p>
      </div>

      <div className="[스타일]">
        <Input
          placeholder="[placeholder]"
          value={query}
          onChange={(e) => setQuery(e.target.value)}
          onKeyDown={(e) => e.key === "Enter" && handleSearch()}
        />
        <Button onClick={handleSearch} disabled={loading}>
          <[아이콘] className="[스타일]" />
          {loading ? "[로딩 텍스트]" : "[기본 텍스트]"}
        </Button>
      </div>

      <Tabs value={searchType} onValueChange={(v) => setSearchType(v as any)}>
        <TabsList>
          <TabsTrigger value="[타입1]">[탭 이름1]</TabsTrigger>
          <TabsTrigger value="[타입2]">[탭 이름2]</TabsTrigger>
        </TabsList>

        <TabsContent value="[타입1]" className="[스타일]">
          <[컴포넌트] results={results} type="[타입1]" />
        </TabsContent>

        <TabsContent value="[타입2]" className="[스타일]">
          <[컴포넌트] results={results} type="[타입2]" />
        </TabsContent>
      </Tabs>
    </div>
  );
}
```

**체크리스트**:
- [ ] [체크 항목 1]
- [ ] [체크 항목 2]
- [ ] [체크 항목 3]
- [ ] [체크 항목 4]
- [ ] [체크 항목 5]

**Testing Checkpoint**:
- [ ] [테스트 항목 1]
- [ ] [테스트 항목 2]
- [ ] [테스트 항목 3]
- [ ] [테스트 항목 4]

**테스트 실행 및 검증**:
```bash
# 단위 테스트 실행
[패키지 관리자] test

# 커버리지 리포트 생성
[패키지 관리자] test:cov

# E2E 테스트 실행 (있는 경우)
[패키지 관리자] test:e2e
```

**📄 TEST-REPORT.md 업데이트**:
- [ ] Week/Day별 테스트 결과 섹션 추가
- [ ] 테스트 통과율 업데이트
- [ ] 커버리지 리포트 추가
- [ ] 테스트 실패 이력 기록 (실패 시)
- [ ] 검증 완료 날짜 기록

---

### Day 13: [작업 주제] ([N-M]시간)

```bash
[패키지 관리자] add [패키지1] [패키지2] [패키지3]
```

```typescript
// [파일 경로]
"use client";

import { useEditor, EditorContent } from "[패키지]";
import [Extension1] from "[패키지]";
import [Extension2] from "[패키지]";
import { Button } from "[경로]";
import {
  [아이콘1],
  [아이콘2],
  [아이콘3],
} from "lucide-react";

interface [Props명] {
  [prop명1]: string;
  [prop명2]: ([변수]: string) => void;
}

export function [컴포넌트명]({ [prop명1], [prop명2] }: [Props명]) {
  const editor = useEditor({
    extensions: [
      [Extension1],
      [Extension2].configure({
        placeholder: "[placeholder]",
      }),
    ],
    content: [prop명1],
    onUpdate: ({ editor }) => {
      [prop명2](editor.getHTML());
    },
  });

  if (!editor) return null;

  return (
    <div className="[스타일]">
      <div className="[스타일]">
        <Button
          variant="ghost"
          size="icon"
          onClick={() => editor.chain().focus().[메서드]().run()}
          className={editor.isActive("[타입]") ? "[활성 스타일]" : ""}
        >
          <[아이콘1] className="[스타일]" />
        </Button>
        <Button
          variant="ghost"
          size="icon"
          onClick={() => editor.chain().focus().[메서드]().run()}
          className={editor.isActive("[타입]") ? "[활성 스타일]" : ""}
        >
          <[아이콘2] className="[스타일]" />
        </Button>
        <Button
          variant="ghost"
          size="icon"
          onClick={() => editor.chain().focus().[메서드]({ level: [레벨] }).run()}
          className={editor.isActive("[타입]") ? "[활성 스타일]" : ""}
        >
          <[아이콘3] className="[스타일]" />
        </Button>
      </div>
      <EditorContent editor={editor} className="[스타일]" />
    </div>
  );
}
```

**체크리스트**:
- [ ] [체크 항목 1]
- [ ] [체크 항목 2]
- [ ] [체크 항목 3]
- [ ] [체크 항목 4]

---

### Day 14: [작업 주제] ([N-M]시간)

```bash
[패키지 관리자] add [패키지명]
```

```typescript
// [파일 경로]
import { [함수] } from "[패키지]";

[함수]({
  apiKey: process.env.[ENV_VAR]!,
  onError: (error) => console.error("[에러 타입]:", error),
});

// [파일 경로]
"use server";

import { auth } from "[패키지]";
import { [함수] } from "[패키지]";

export async function [함수명](
  [파라미터1]: string,
  [파라미터2]: string
) {
  const { userId } = await auth();
  if (!userId) throw new Error("Unauthorized");

  try {
    const [변수명] = await [함수](
      process.env.[ENV_VAR1]!,
      [파라미터2],
      {
        checkoutData: {
          custom: {
            [필드명1]: [파라미터1],
            [필드명2]: userId,
          },
        },
      }
    );

    return [변수명].data?.attributes.url;
  } catch (error) {
    console.error("[에러 타입]:", error);
    throw error;
  }
}
```

**체크리스트**:
- [ ] [체크 항목 1]
- [ ] [체크 항목 2]
- [ ] [체크 항목 3]
- [ ] [체크 항목 4]
- [ ] [체크 항목 5]

---

### Day 15: [작업 주제] ([N-M]시간)

#### Task 10.1: [작업 이름]
- [ ] [작업 1]
- [ ] [작업 2]
- [ ] [작업 3]
- [ ] [작업 4]

#### Task 10.2: [작업 이름]
```bash
# [도구] 설치
[패키지 관리자] i -g [도구명]

# 배포
[도구명]

# [환경] 배포
[도구명] --[플래그]
```

**환경 변수 설정 ([플랫폼] Dashboard)**:
```
[ENV_VAR_1]
[ENV_VAR_2]
[ENV_VAR_3]
[ENV_VAR_4]
[ENV_VAR_5]
[ENV_VAR_6]
[ENV_VAR_7]
```

**체크리스트**:
- [ ] [체크 항목 1]
- [ ] [체크 항목 2]
- [ ] [체크 항목 3]
- [ ] [체크 항목 4]
- [ ] [체크 항목 5]
- [ ] [체크 항목 6]

---

## 마일스톤 및 체크포인트

### Milestone 1: [마일스톤 이름] (Week 1 완료)
**완료 기준**:
- [ ] [기준 1]
- [ ] [기준 2]
- [ ] [기준 3]
- [ ] [기준 4]

### Milestone 2: [마일스톤 이름] (Week 2 완료)
**완료 기준**:
- [ ] [기준 1]
- [ ] [기준 2]
- [ ] [기준 3]
- [ ] [기준 4]

### Milestone 3: [마일스톤 이름] (Week 3 완료)
**완료 기준**:
- [ ] [기준 1]
- [ ] [기준 2]
- [ ] [기준 3]
- [ ] [기준 4]
- [ ] [기준 5]

---

## 리스크 관리

### 기술적 리스크

| 리스크 | 영향도 | 대응 전략 |
|--------|--------|----------|
| [리스크 1] | [높음/중간/낮음] | - [대응 1]<br>- [대응 2]<br>- [대응 3] |
| [리스크 2] | [높음/중간/낮음] | - [대응 1]<br>- [대응 2]<br>- [대응 3] |
| [리스크 3] | [높음/중간/낮음] | - [대응 1]<br>- [대응 2]<br>- [대응 3] |
| [리스크 4] | [높음/중간/낮음] | - [대응 1]<br>- [대응 2] |

### 일정 리스크

| 리스크 | 확률 | 대응 전략 |
|--------|------|----------|
| [리스크 1] | [높음/중간/낮음] | - [대응 1]<br>- [대응 2] |
| [리스크 2] | [높음/중간/낮음] | - [대응 1]<br>- [대응 2] |
| [리스크 3] | [높음/중간/낮음] | - [대응 1]<br>- [대응 2] |

---

## 성공 기준

### 기능적 성공 기준
- [ ] [기준 1]
- [ ] [기준 2]
- [ ] [기준 3]
- [ ] [기준 4]
- [ ] [기준 5]

### 기술적 성공 기준
- [ ] [기준 1]
- [ ] [기준 2]
- [ ] [기준 3]
- [ ] [기준 4]
- [ ] [기준 5]

### 배포 성공 기준
- [ ] [기준 1]
- [ ] [기준 2]
- [ ] [기준 3]
- [ ] [기준 4]

---

## 일일 체크리스트 템플릿

```markdown
## Day X: [Task Name]

### Morning Tasks
- [ ] Task 1
- [ ] Task 2
- [ ] Task 3

### Afternoon Tasks
- [ ] Task 4
- [ ] Task 5

### Testing
- [ ] Unit tests 실행 및 통과
- [ ] Integration tests 실행 및 통과
- [ ] E2E tests (필요 시)
- [ ] 커버리지 확인 ([N]%+)
- [ ] TEST-REPORT.md 업데이트

**테스트 명령어**:
```bash
[패키지 관리자] test           # 단위 테스트
[패키지 관리자] test:cov       # 커버리지
[패키지 관리자] test:e2e       # E2E 테스트
```

### Blockers
- None

### Notes
- [Add any notes or learnings]
```

---

## 완료 후 Next Steps

1. **[단계 1]**
2. **[단계 2]** ([도구 1], [도구 2])
3. **[단계 3]** ([문서 타입 1], [문서 타입 2])
4. **[단계 4]** ([기능 1], [기능 2], [기능 3])
5. **[단계 5]** ([다음 프로젝트 설명])

---

**프로젝트 시작일**: [날짜]
**예상 완료일**: [날짜]
**실제 완료일**: TBD

---

## 환경 변수 요약

**이 섹션은 ENV-SETUP.md에서 설정한 환경 변수의 요약입니다.**

### Backend 환경 변수

```bash
# Database
DATABASE_URL="[연결 문자열]"
DIRECT_URL="[Direct 연결 문자열]"

# BaaS (예: Supabase)
SUPABASE_URL="[URL]"
SUPABASE_ANON_KEY="[Key]"
SUPABASE_SERVICE_ROLE_KEY="[Key]"

# Auth (예: Clerk)
CLERK_PUBLISHABLE_KEY="[Key]"
CLERK_SECRET_KEY="[Key]"
CLERK_WEBHOOK_SECRET="[Secret]"

# AI
OPENAI_API_KEY="[Key]"
ANTHROPIC_API_KEY="[Key]"

# App
NODE_ENV="development"
PORT=[포트]
FRONTEND_URL="http://localhost:[포트]"

# Security
JWT_SECRET="[Secret]"
SESSION_SECRET="[Secret]"

# Monitoring
SENTRY_DSN="[DSN]"
```

### Frontend 환경 변수

```bash
# API
VITE_API_URL="http://localhost:[포트]"
VITE_BACKEND_URL="http://localhost:[포트]"

# BaaS
VITE_SUPABASE_URL="[URL]"
VITE_SUPABASE_ANON_KEY="[Key]"

# Auth
VITE_CLERK_PUBLISHABLE_KEY="[Key]"

# Monitoring
VITE_SENTRY_DSN="[DSN]"
```

**📄 상세 설정은 [ENV-SETUP.md](./ENV-SETUP.md) 참조**

---

이 계획서는 유동적이며, 진행 상황에 따라 조정될 수 있습니다.
