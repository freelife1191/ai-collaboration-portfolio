# 저수준 설계 문서 (LLD)
## [프로젝트 이름] - [프로젝트 설명]

**문서 버전**: [버전]
**작성일**: [작성일]
**작성자**: [작성자]

---

## 📋 목차

0. [개발 시작 전 환경 설정 체크](#개발-시작-전-환경-설정-체크) ⭐ **최우선 필수**
1. [개발 프로세스 및 테스트 요구사항](#개발-프로세스-및-테스트-요구사항) ⚠️ **필수**
2. [시스템 개요](#시스템-개요)
3. [기술 스택 상세](#기술-스택-상세)
4. [컴포넌트 설계](#컴포넌트-설계)
5. [데이터 구조 및 알고리즘](#데이터-구조-및-알고리즘)
6. [함수 시그니처](#함수-시그니처)
7. [에러 처리 전략](#에러-처리-전략)
8. [캐싱 전략](#캐싱-전략)
9. [보안 구현](#보안-구현)
10. [성능 최적화](#성능-최적화)
11. [테스트 전략](#테스트-전략)

---

## ⭐ 개발 시작 전 환경 설정 체크

### 🔴 필수: 이 섹션을 먼저 완료하지 않으면 개발 시작 불가!

**이 LLD 문서에 따라 개발을 시작하기 전에, 반드시 프로젝트에 필요한 환경 설정이 완료되어야 합니다.**

---

### 📋 Step 1: 이 프로젝트에 필요한 환경 요소 체크

**아래 표를 프로젝트 상황에 맞게 체크하세요:**

| 환경 요소 | 필요 여부 | 서비스/도구명 | 설정 완료 |
|----------|----------|-------------|----------|
| **로컬 개발 도구** | | | |
| Node.js | [ ] 필요 / [ ] 불필요 | [버전: ] | [ ] |
| Python | [ ] 필요 / [ ] 불필요 | [버전: ] | [ ] |
| Docker | [ ] 필요 / [ ] 불필요 | [버전: ] | [ ] |
| Git | [ ] 필요 / [ ] 불필요 | [버전: ] | [ ] |
| **데이터베이스** | | | |
| PostgreSQL | [ ] 필요 / [ ] 불필요 | [로컬/클라우드: ] | [ ] |
| MySQL | [ ] 필요 / [ ] 불필요 | [로컬/클라우드: ] | [ ] |
| MongoDB | [ ] 필요 / [ ] 불필요 | [로컬/클라우드: ] | [ ] |
| Redis | [ ] 필요 / [ ] 불필요 | [로컬/클라우드: ] | [ ] |
| **BaaS (Backend as a Service)** | | | |
| Supabase | [ ] 필요 / [ ] 불필요 | [프로젝트: ] | [ ] |
| Firebase | [ ] 필요 / [ ] 불필요 | [프로젝트: ] | [ ] |
| Convex | [ ] 필요 / [ ] 불필요 | [프로젝트: ] | [ ] |
| Appwrite | [ ] 필요 / [ ] 불필요 | [프로젝트: ] | [ ] |
| **인증 서비스** | | | |
| Clerk | [ ] 필요 / [ ] 불필요 | [App: ] | [ ] |
| Auth0 | [ ] 필요 / [ ] 불필요 | [App: ] | [ ] |
| Firebase Auth | [ ] 필요 / [ ] 불필요 | [프로젝트: ] | [ ] |
| Supabase Auth | [ ] 필요 / [ ] 불필요 | [프로젝트: ] | [ ] |
| NextAuth.js | [ ] 필요 / [ ] 불필요 | - | [ ] |
| **OAuth 소셜 로그인** | | | |
| Google OAuth | [ ] 필요 / [ ] 불필요 | [Client ID: ] | [ ] |
| Kakao OAuth | [ ] 필요 / [ ] 불필요 | [앱 키: ] | [ ] |
| Naver OAuth | [ ] 필요 / [ ] 불필요 | [Client ID: ] | [ ] |
| GitHub OAuth | [ ] 필요 / [ ] 불필요 | [Client ID: ] | [ ] |
| **AI/LLM 서비스** | | | |
| OpenAI | [ ] 필요 / [ ] 불필요 | [사용 모델: ] | [ ] |
| Anthropic (Claude) | [ ] 필요 / [ ] 불필요 | [사용 모델: ] | [ ] |
| Google Gemini | [ ] 필요 / [ ] 불필요 | [사용 모델: ] | [ ] |
| **결제 서비스** | | | |
| Stripe | [ ] 필요 / [ ] 불필요 | [Test/Live: ] | [ ] |
| Toss Payments | [ ] 필요 / [ ] 불필요 | [Test/Live: ] | [ ] |
| Lemon Squeezy | [ ] 필요 / [ ] 불필요 | [Test/Live: ] | [ ] |
| PayPal | [ ] 필요 / [ ] 불필요 | [Test/Live: ] | [ ] |
| 아임포트 (PortOne) | [ ] 필요 / [ ] 불필요 | [가맹점: ] | [ ] |
| **배포 플랫폼** | | | |
| Vercel | [ ] 필요 / [ ] 불필요 | [프로젝트: ] | [ ] |
| Railway | [ ] 필요 / [ ] 불필요 | [프로젝트: ] | [ ] |
| Render | [ ] 필요 / [ ] 불필요 | [프로젝트: ] | [ ] |
| AWS | [ ] 필요 / [ ] 불필요 | [서비스: ] | [ ] |
| **모니터링** | | | |
| Sentry | [ ] 필요 / [ ] 불필요 | [프로젝트: ] | [ ] |
| LogRocket | [ ] 필요 / [ ] 불필요 | [프로젝트: ] | [ ] |
| Datadog | [ ] 필요 / [ ] 불필요 | [프로젝트: ] | [ ] |
| **MCP 서버** | | | |
| Filesystem MCP | [ ] 필요 / [ ] 불필요 | - | [ ] |
| Database MCP | [ ] 필요 / [ ] 불필요 | [DB 종류: ] | [ ] |
| GitHub MCP | [ ] 필요 / [ ] 불필요 | - | [ ] |
| Custom MCP | [ ] 필요 / [ ] 불필요 | [설명: ] | [ ] |
| **기타 서비스** | | | |
| [서비스명] | [ ] 필요 / [ ] 불필요 | [상세: ] | [ ] |
| [서비스명] | [ ] 필요 / [ ] 불필요 | [상세: ] | [ ] |

---

### 📋 Step 2: API 키 및 토큰 수집 체크리스트

**위 표에서 "필요"로 체크한 서비스들의 API 키를 수집하세요:**

| 서비스 | 환경 변수명 | API 키 수집 완료 | `.env` 파일 저장 완료 |
|--------|------------|-----------------|---------------------|
| [서비스명 1] | `[ENV_VAR_1]` | [ ] | [ ] |
| [서비스명 2] | `[ENV_VAR_2]` | [ ] | [ ] |
| [서비스명 3] | `[ENV_VAR_3]` | [ ] | [ ] |
| [서비스명 4] | `[ENV_VAR_4]` | [ ] | [ ] |
| [서비스명 5] | `[ENV_VAR_5]` | [ ] | [ ] |
| [서비스명 6] | `[ENV_VAR_6]` | [ ] | [ ] |

**⚠️ 보안 주의:**
- [ ] `.env` 파일이 `.gitignore`에 포함됨
- [ ] `.env.example` 파일 생성 (값 제외, 키 이름만)
- [ ] API 키를 코드에 하드코딩하지 않음
- [ ] 민감한 키는 서버 환경에서만 사용 (클라이언트 노출 금지)

---

### 📋 Step 3: 환경 변수 파일 준비

**프로젝트에 필요한 `.env` 파일 구조를 작성하세요:**

#### Backend `.env` 예시
```bash
# Database
DATABASE_URL="[연결 문자열]"

# BaaS (예: Supabase)
SUPABASE_URL="[URL]"
SUPABASE_ANON_KEY="[Key]"
SUPABASE_SERVICE_ROLE_KEY="[Key]"

# Auth (예: Clerk)
CLERK_SECRET_KEY="[Key]"

# AI
OPENAI_API_KEY="[Key]"

# App
NODE_ENV="development"
PORT=[포트]

# Security
JWT_SECRET="[Secret - openssl rand -base64 32]"

# Monitoring
SENTRY_DSN="[DSN]"
```

#### Frontend `.env.local` 예시
```bash
# API
VITE_API_URL="http://localhost:[포트]"

# BaaS
VITE_SUPABASE_URL="[URL]"
VITE_SUPABASE_ANON_KEY="[Key]"

# Auth
VITE_CLERK_PUBLISHABLE_KEY="[Key]"

# Monitoring
VITE_SENTRY_DSN="[DSN]"
```

**체크리스트:**
- [ ] Backend `.env` 파일 생성 완료
- [ ] Frontend `.env.local` 파일 생성 완료
- [ ] 모든 필수 환경 변수 입력 완료
- [ ] `.env.example` 파일 커밋 완료

---

### 📋 Step 4: 환경 검증

**개발을 시작하기 전에 환경이 올바르게 설정되었는지 검증하세요:**

#### 로컬 개발 환경 테스트

**Backend 검증:**
```bash
# 의존성 설치
[패키지 관리자] install

# 환경 변수 로드 확인
[패키지 관리자] run dev
# 또는
node -e "console.log(process.env.DATABASE_URL ? '✅ ENV loaded' : '❌ ENV not loaded')"

# 데이터베이스 연결 테스트
[패키지 관리자] run db:test
# 또는 Prisma 사용 시
npx prisma db push
```

**Frontend 검증:**
```bash
# 의존성 설치
[패키지 관리자] install

# 개발 서버 실행
[패키지 관리자] run dev

# 브라우저 콘솔에서 환경 변수 확인
# (주의: VITE_ 접두사가 있는 변수만 클라이언트에서 접근 가능)
```

#### 서비스 연결 테스트

**체크리스트:**
- [ ] 데이터베이스 연결 성공 (쿼리 실행 테스트)
- [ ] BaaS 연결 성공 (간단한 CRUD 테스트)
- [ ] 인증 서비스 연결 성공 (테스트 로그인)
- [ ] AI API 연결 성공 (간단한 요청 테스트)
- [ ] 배포 플랫폼 CLI 인증 성공
- [ ] MCP 서버 연동 성공 (Claude Desktop 재시작 후 확인)

---

### 📋 Step 5: 최종 환경 설정 체크리스트

**모든 항목을 체크한 후에만 개발을 시작하세요:**

- [ ] **Step 1 완료**: 필요한 모든 환경 요소 확인 및 설치
- [ ] **Step 2 완료**: 모든 필요한 API 키 수집 및 저장
- [ ] **Step 3 완료**: `.env` 파일 생성 및 검증
- [ ] **Step 4 완료**: 환경 검증 테스트 전체 통과
- [ ] **ENV-SETUP.md 완료**: 전체 환경 설정 가이드 완료 확인
- [ ] **README.md 업데이트**: 환경 설정 가이드 작성
- [ ] **팀 공유**: 팀원들에게 환경 설정 방법 공유

**✅ 완료 확인 날짜**: [YYYY-MM-DD]

---

### 🚨 환경 설정 미완료 시 발생 가능한 문제

**환경 설정을 건너뛰고 개발을 시작하면:**

1. ❌ 개발 중간에 API 키가 없어서 막힘
2. ❌ 데이터베이스 연결 실패로 시간 낭비
3. ❌ 배포 시 환경 변수 누락으로 서비스 장애
4. ❌ 팀원 온보딩 시 환경 설정 미흡으로 혼란
5. ❌ 보안 키 노출로 인한 보안 사고

**⚠️ 반드시 모든 환경 설정을 완료한 후 개발을 시작하세요!**

---

### 📚 참고 문서

- **상세 환경 설정 가이드**: [ENV-SETUP.md](./ENV-SETUP.md)
- **프로젝트 계획**: [PLAN.md](./PLAN.md)
- **체크리스트**: [CHECKLIST.md](./CHECKLIST.md)

---

## ⚠️ 개발 프로세스 및 테스트 요구사항

### 🔴 필수 준수 사항

**이 프로젝트는 테스트 기반 개발(Test-Driven Development) 방법론을 따릅니다.**

모든 개발 작업은 다음 프로세스를 **반드시** 준수해야 하며, 이 규칙을 위반하여 진행된 개발은 코드 리뷰에서 거부됩니다.

---

### 📝 개발 단위별 테스트 요구사항

#### 1️⃣ 개발 단위 정의
하나의 개발 단위는 다음 중 하나를 의미합니다:
- **컴포넌트 단위**: 하나의 React 컴포넌트 완성
- **함수 단위**: 하나의 유틸리티 함수 또는 Server Action 완성
- **API 단위**: 하나의 API 엔드포인트 완성
- **모듈 단위**: 관련된 기능들의 집합 (예: 인증 모듈, 파일 업로드 모듈)

#### 2️⃣ 필수 테스트 통과 조건

각 개발 단위는 다음 테스트를 **100% 통과**해야만 다음 단계로 진행할 수 있습니다:

```
┌─────────────────────────────────────────────────────┐
│  개발 단위 N 시작                                    │
│                                                     │
│  1. 코드 작성                                       │
│  2. Unit Test 작성 및 실행                         │
│  3. ✅ 모든 Unit Test PASS 확인                    │
│  4. 코드 리뷰 (선택사항)                            │
│  5. ✅ 테스트 통과 후 다음 단위로 진행             │
└─────────────────────────────────────────────────────┘
         │
         ▼
┌─────────────────────────────────────────────────────┐
│  개발 단위 N+1 시작                                 │
└─────────────────────────────────────────────────────┘
```

**❌ 금지 사항:**
- Unit Test가 실패한 상태에서 다음 개발 단위 작업 시작
- "나중에 테스트 작성하겠습니다" 방식의 개발
- 테스트 없이 기능만 구현하는 행위

---

### 📝 커밋 규칙 및 전략

#### 커밋 전 필수 사항
- **CHECKLIST.md 업데이트 필수**: 각 커밋 전에 진행상황을 `CHECKLIST.md`에 반영
- **명확하고 간결한 커밋 메시지**: 변경 내용을 한눈에 알 수 있도록 작성
- **브랜치 전략 준수**:
  - `feature/기능명`: 새로운 기능 개발
  - `bugfix/버그명`: 버그 수정
  - `hotfix/긴급수정명`: 긴급 패치
  - `refactor/리팩토링명`: 코드 리팩토링

#### 커밋 메시지 컨벤션

```bash
# 형식
<type>(<scope>): <subject>

# 예시
feat(auth): 사용자 로그인 기능 추가
fix(api): 데이터 조회 시 null 처리 오류 수정
test(user): 사용자 생성 Unit Test 추가
refactor(components): Button 컴포넌트 Props 타입 개선
docs(readme): 설치 가이드 업데이트
```

**Type 종류**:
- `feat`: 새로운 기능
- `fix`: 버그 수정
- `test`: 테스트 코드 추가/수정
- `refactor`: 코드 리팩토링
- `docs`: 문서 수정
- `style`: 코드 스타일 변경 (포맷팅)
- `chore`: 빌드 프로세스 또는 보조 도구 변경
- `perf`: 성능 개선

---

### 🎯 Git Hook을 통한 자동화된 코드 품질 관리

**Git Hook을 사용하여 커밋/푸시 시 자동으로 코드 품질을 검증합니다.**

#### 🔧 Pre-commit Hook (커밋 전 실행)

**위치**: `.husky/pre-commit` (모노레포의 경우 루트에 위치)

**스마트 감지**: frontend/backend 디렉토리 변경 시에만 해당 hook 실행

**자동 실행 순서**:

1. **🔧 Lint Fix**: ESLint를 통한 코드 스타일 자동 수정
   ```bash
   npm run lint:fix
   # 또는
   pnpm lint:fix
   ```

2. **🏗️ Build**: TypeScript 컴파일 및 빌드 검증
   ```bash
   npm run build
   # 또는
   pnpm build
   ```

3. **🧪 Unit Test**: Vitest/Jest를 통한 테스트 실행
   ```bash
   npm run test:unit
   # 또는
   pnpm test:unit
   ```

**Pre-commit Hook 예시 (`.husky/pre-commit`)**:

```bash
#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# 변경된 파일 감지
CHANGED_FILES=$(git diff --cached --name-only)

# frontend 디렉토리 변경 확인 (모노레포)
if echo "$CHANGED_FILES" | grep -q "^frontend/"; then
  echo "🔍 Frontend changes detected. Running pre-commit checks..."

  cd frontend || exit 1

  # 1. Lint Fix
  echo "🔧 Running ESLint fix..."
  npm run lint:fix || exit 1

  # 2. Build
  echo "🏗️ Running build..."
  npm run build || exit 1

  # 3. Unit Tests
  echo "🧪 Running unit tests..."
  npm run test:unit || exit 1

  echo "✅ All pre-commit checks passed!"
fi

# 단일 레포지토리인 경우
# echo "🔍 Running pre-commit checks..."
# npm run lint:fix || exit 1
# npm run build || exit 1
# npm run test:unit || exit 1
# echo "✅ All pre-commit checks passed!"
```

---

#### 🎭 Pre-push Hook (푸시 전 실행)

**위치**: `.husky/pre-push` (모노레포의 경우 루트에 위치)

**스마트 감지**: frontend/backend 디렉토리 변경이 포함된 push 시에만 실행

**자동 실행 순서**:

1. **🏗️ Build**: TypeScript 컴파일 및 빌드 검증
   ```bash
   npm run build
   # 또는
   pnpm build
   ```

2. **🎭 E2E Test**: Playwright를 통한 E2E 테스트 실행
   ```bash
   npm run test:e2e
   # 또는
   pnpm test:e2e
   ```

**⚠️ 중요**: E2E 테스트는 **로컬에서만 실행**되며, GitHub Actions에서는 비용 문제로 실행하지 않습니다.

**Pre-push Hook 예시 (`.husky/pre-push`)**:

```bash
#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# 현재 브랜치와 remote 브랜치 비교하여 변경된 파일 확인
BRANCH=$(git rev-parse --abbrev-ref HEAD)
REMOTE_BRANCH="origin/$BRANCH"

# remote 브랜치가 없으면 HEAD와 비교
if git rev-parse --verify "$REMOTE_BRANCH" >/dev/null 2>&1; then
  CHANGED_FILES=$(git diff --name-only "$REMOTE_BRANCH" HEAD)
else
  CHANGED_FILES=$(git diff --name-only HEAD)
fi

# frontend 디렉토리 변경 확인 (모노레포)
if echo "$CHANGED_FILES" | grep -q "^frontend/"; then
  echo "🔍 Frontend changes detected in push. Running pre-push checks..."

  cd frontend || exit 1

  # 1. Build
  echo "🏗️ Running build..."
  npm run build || exit 1

  # 2. E2E Tests (로컬에서만 실행)
  echo "🎭 Running E2E tests locally..."
  echo "⚠️  E2E tests will NOT run in CI/CD (cost optimization)"
  npm run test:e2e || exit 1

  echo "✅ All pre-push checks passed!"
fi

# 단일 레포지토리인 경우
# echo "🔍 Running pre-push checks..."
# npm run build || exit 1
# echo "🎭 Running E2E tests locally..."
# npm run test:e2e || exit 1
# echo "✅ All pre-push checks passed!"
```

---

#### 📝 Git Hook 사용 예시

**시나리오 1: Frontend 파일 변경 후 커밋**

```bash
# 1. 파일 수정
vim frontend/src/components/MyComponent.tsx

# 2. Git add
git add frontend/src/components/MyComponent.tsx

# 3. Git commit (pre-commit hook 자동 실행)
git commit -m "feat(components): MyComponent 추가"

# → 자동으로 다음 순서로 실행:
# 🔧 ESLint fix
# 🏗️ Build
# 🧪 Unit Test
# ✅ 모두 통과하면 커밋 완료
```

**시나리오 2: Frontend 변경사항 푸시**

```bash
# 1. Git push (pre-push hook 자동 실행)
git push origin feature/my-feature

# → 자동으로 다음 순서로 실행:
# 🏗️ Build
# 🎭 E2E Test (로컬에서만)
# ✅ 모두 통과하면 푸시 완료
```

**시나리오 3: 다른 디렉토리 변경 시 (Hook 실행 안됨)**

```bash
# 1. 문서 파일 수정
git add docs/README.md
git commit -m "docs: README 업데이트"

# → frontend 변경 없으므로 hook 실행 안됨 (빠른 커밋)

git push origin main

# → frontend 변경 없으므로 E2E 테스트 실행 안됨 (빠른 푸시)
```

---

#### 🛠️ Git Hook 설치 가이드

**1. Husky 설치**:

```bash
# npm
npm install --save-dev husky

# pnpm
pnpm add -D husky

# 초기화
npx husky install

# package.json에 prepare 스크립트 추가
npm pkg set scripts.prepare="husky install"
```

**2. Pre-commit Hook 생성**:

```bash
npx husky add .husky/pre-commit "npm run pre-commit"
```

**3. Pre-push Hook 생성**:

```bash
npx husky add .husky/pre-push "npm run pre-push"
```

**4. package.json 스크립트 추가**:

```json
{
  "scripts": {
    "prepare": "husky install",
    "pre-commit": "npm run lint:fix && npm run build && npm run test:unit",
    "pre-push": "npm run build && npm run test:e2e",
    "lint:fix": "eslint . --fix",
    "build": "tsc && vite build",
    "test:unit": "vitest run",
    "test:e2e": "playwright test"
  }
}
```

---

#### ⚡ Hook 스킵 방법 (긴급 상황)

**긴급하게 hook을 건너뛰어야 하는 경우** (권장하지 않음):

```bash
# Pre-commit hook 스킵
git commit -m "feat: 긴급 수정" --no-verify

# Pre-push hook 스킵
git push origin main --no-verify
```

**⚠️ 주의**: Hook을 스킵하면 코드 품질 검증이 우회되므로, 반드시 나중에 수동으로 테스트를 실행하세요.

---

#### 📊 Hook 실행 플로우 다이어그램

```
┌─────────────────────────────────────────────────────┐
│  git commit                                          │
└──────────────┬──────────────────────────────────────┘
               │
               ▼
     ┌─────────────────┐
     │  Pre-commit     │
     │  Hook 실행      │
     └────┬────────────┘
          │
          ├─> 🔧 Lint Fix
          │   └─> ❌ 실패 → 커밋 취소
          │   └─> ✅ 성공
          │
          ├─> 🏗️ Build
          │   └─> ❌ 실패 → 커밋 취소
          │   └─> ✅ 성공
          │
          └─> 🧪 Unit Test
              └─> ❌ 실패 → 커밋 취소
              └─> ✅ 성공 → 커밋 완료
                              │
                              ▼
                    ┌─────────────────┐
                    │  git push        │
                    └────┬─────────────┘
                         │
                         ▼
                ┌─────────────────┐
                │  Pre-push       │
                │  Hook 실행      │
                └────┬────────────┘
                     │
                     ├─> 🏗️ Build
                     │   └─> ❌ 실패 → 푸시 취소
                     │   └─> ✅ 성공
                     │
                     └─> 🎭 E2E Test (로컬)
                         └─> ❌ 실패 → 푸시 취소
                         └─> ✅ 성공 → 푸시 완료
```

---

#### 💡 Git Hook 모범 사례

**✅ DO (권장 사항)**:
1. **정기적인 커밋**: 작은 단위로 자주 커밋하여 hook 실행 시간 단축
2. **테스트 작성**: 커밋 전에 Unit Test를 먼저 작성하고 통과시키기
3. **Hook 신뢰**: Hook이 실패하면 반드시 수정 후 재시도
4. **로컬 E2E**: 푸시 전 로컬에서 E2E 테스트 실행으로 프로덕션 안정성 보장

**❌ DON'T (피해야 할 사항)**:
1. **Hook 우회 남용**: `--no-verify` 옵션 남용 금지
2. **테스트 미작성**: 기능 구현 후 테스트 없이 커밋 금지
3. **대량 변경**: 한 번에 너무 많은 파일을 커밋하여 hook 실행 시간 증가
4. **Hook 무시**: Hook 실패를 무시하고 강제로 푸시 금지

---

### 🎯 전체 개발 완료 후 테스트 요구사항

모든 개발 단위가 완료된 후, **반드시** 다음 테스트들을 순차적으로 수행해야 합니다:

#### 1️⃣ Integration Test (통합 테스트)
```bash
# 예시: Vitest를 사용하는 경우
npm run test:integration

# 또는
pnpm test:integration
```

**통합 테스트 대상:**
- 여러 컴포넌트 간의 상호작용
- Server Action과 Database 간의 통합
- API 엔드포인트와 클라이언트 간의 통합
- 외부 서비스(Supabase, OpenAI 등)와의 연동

**통과 기준:**
- ✅ 모든 Integration Test PASS (100%)
- ✅ 커버리지: 최소 80% 이상

---

#### 2️⃣ E2E Test (End-to-End 테스트)
```bash
# 예시: Playwright를 사용하는 경우
npm run test:e2e

# 또는
pnpm playwright test
```

**E2E 테스트 대상:**
- 사용자 시나리오 기반 전체 플로우
- 실제 브라우저에서의 UI/UX 검증
- 인증 플로우 (로그인, 로그아웃, 회원가입)
- 핵심 비즈니스 로직 플로우

**통과 기준:**
- ✅ 모든 E2E Test PASS (100%)
- ✅ 크리티컬 패스 시나리오 100% 커버

---

### 📚 테스트 전략 참고 문서

**모든 테스트 작성 시 다음 문서의 전략을 반드시 적용하세요:**

👉 **[@TEST-VERIFY-TEMPLATE.md](./TEST-VERIFY-TEMPLATE.md)**

이 문서는 다음 내용을 포함합니다:
- ✅ Unit Test 작성 가이드 (Vitest, Jest, React Testing Library)
- ✅ Integration Test 작성 가이드
- ✅ E2E Test 작성 가이드 (Playwright)
- ✅ 테스트 커버리지 목표 및 측정 방법
- ✅ CI/CD 파이프라인 통합 전략
- ✅ 테스트 더블(Mock, Stub, Spy) 사용 전략

---

### 🔄 개발 워크플로우 체크리스트

각 개발 단위 작업 시 다음 체크리스트를 따르세요:

#### Phase 1: 계획
- [ ] 개발 단위 정의 및 범위 설정
- [ ] 테스트 케이스 설계 (TDD의 경우 테스트 먼저 작성)

#### Phase 2: 개발
- [ ] 코드 작성
- [ ] Unit Test 작성
- [ ] **Unit Test 실행 및 100% 통과 확인** ⚠️ **필수**
- [ ] 코드 리뷰 요청 (선택)

#### Phase 3: 검증
- [ ] ESLint/Prettier 통과 확인
- [ ] TypeScript 타입 체크 통과 확인
- [ ] Git commit 및 push

#### Phase 4: 다음 단계
- [ ] ✅ 모든 테스트 통과 확인 후 다음 개발 단위 시작

---

#### 전체 개발 완료 후 체크리스트

- [ ] **모든 Unit Test PASS** ✅
- [ ] **Integration Test 작성 및 PASS** ✅
- [ ] **E2E Test 작성 및 PASS** ✅
- [ ] 테스트 커버리지 확인 (Unit: 80%+, Integration: 70%+)
- [ ] CI/CD 파이프라인에서 자동 테스트 실행 확인
- [ ] 배포 전 최종 검증

---

### 🚨 테스트 실패 시 대응 절차

**테스트가 실패했을 때:**

1. **즉시 개발 중단**: 새로운 기능 개발을 멈추고 실패한 테스트를 수정합니다.
2. **원인 분석**: 왜 테스트가 실패했는지 로그 및 에러 메시지를 분석합니다.
3. **수정 및 재실행**: 코드를 수정하고 테스트를 다시 실행합니다.
4. **회귀 테스트**: 수정 후 전체 테스트 스위트를 다시 실행하여 다른 테스트가 깨지지 않았는지 확인합니다.
5. **문서화**: 실패 원인과 해결 방법을 문서화합니다 (TROUBLESHOOTING 섹션에 추가).

---

### 📊 테스트 커버리지 목표

| 테스트 유형 | 최소 커버리지 | 권장 커버리지 |
|------------|--------------|--------------|
| Unit Test | 80% | 90%+ |
| Integration Test | 70% | 80%+ |
| E2E Test (Critical Path) | 100% | 100% |

**커버리지 측정 명령:**
```bash
# Unit + Integration 커버리지
npm run test:coverage

# 상세 리포트 확인
open coverage/index.html
```

---

### ⚡ CI/CD 파이프라인 자동 테스트

**모든 Pull Request는 다음 테스트를 자동으로 실행하며, 실패 시 Merge가 차단됩니다:**

```yaml
# .github/workflows/test.yml 예시
name: Test

on: [pull_request]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install dependencies
        run: pnpm install
      - name: Run Unit Tests
        run: pnpm test:unit
      - name: Run Integration Tests
        run: pnpm test:integration
      - name: Run E2E Tests
        run: pnpm test:e2e
      - name: Check Coverage
        run: pnpm test:coverage
```

---

### 📖 참고 자료

- **TDD (Test-Driven Development)**: 테스트 먼저 작성 → 코드 작성 → 리팩토링
- **BDD (Behavior-Driven Development)**: 사용자 행동 기반 테스트 작성
- **Testing Pyramid**: Unit (70%) > Integration (20%) > E2E (10%)

---

**이 섹션의 규칙은 프로젝트의 코드 품질과 안정성을 보장하기 위해 절대적으로 준수되어야 합니다.**

---

## 시스템 개요

### 아키텍처 레이어

```
┌──────────────────────────────────────────┐
│         [레이어 1 이름]                   │
│  ([프레임워크] + [기술 스택])             │
├──────────────────────────────────────────┤
│         [레이어 2 이름]                   │
│     ([컴포넌트] + [도구])                │
├──────────────────────────────────────────┤
│         [레이어 3 이름]                   │
│  ([클라이언트] + [클라이언트])            │
├──────────────────────────────────────────┤
│         [레이어 4 이름]                   │
│  ([서비스], [서비스], [서비스])           │
└──────────────────────────────────────────┘
```

### 디렉토리 구조 상세

```
[루트 디렉토리]/
├── [디렉토리 1]/                         # [설명]
│   ├── [하위 디렉토리 1]/               # [라우트 그룹 설명]
│   │   ├── [경로]/
│   │   │   └── [[...[동적]]]/
│   │   │       └── [파일명]             # [설명]
│   │   └── [경로]/
│   │       └── [[...[동적]]]/
│   │           └── [파일명]             # [설명]
│   ├── [하위 디렉토리 2]/               # [라우트 그룹 설명]
│   │   ├── [파일명]                     # [공통 레이아웃]
│   │   ├── [파일명]                     # [목록 페이지]
│   │   └── [동적 경로]/                 # [동적 라우트]
│   │       ├── [파일명]                 # [레이아웃]
│   │       ├── [파일명]                 # [홈 페이지]
│   │       ├── [경로]/
│   │       │   ├── [동적 ID]/
│   │       │   │   └── [파일명]         # [상세 페이지]
│   │       │   └── [경로]/
│   │       │       └── [파일명]         # [새 항목 페이지]
│   │       ├── [경로]/
│   │       │   └── [파일명]             # [기능 페이지]
│   │       ├── [경로]/
│   │       │   └── [파일명]             # [설정 페이지]
│   │       └── [경로]/
│   │           └── [파일명]             # [관리 페이지]
│   ├── [디렉토리 2]/                    # [API Routes]
│   │   ├── [경로]/
│   │   │   ├── [서비스]/
│   │   │   │   └── [파일명]             # [웹훅 설명]
│   │   │   └── [서비스]/
│   │   │       └── [파일명]             # [웹훅 설명]
│   │   └── [경로]/
│   │       └── [작업]/
│   │           └── [파일명]             # [크론 작업]
│   ├── [파일명]                         # [루트 레이아웃]
│   ├── [파일명]                         # [랜딩 페이지]
│   └── [파일명]                         # [글로벌 스타일]
│
├── [디렉토리 3]/                        # [React 컴포넌트]
│   ├── [하위 디렉토리]/                 # [기본 컴포넌트]
│   │   ├── [파일명]
│   │   ├── [파일명]
│   │   ├── [파일명]
│   │   ├── [파일명]
│   │   ├── [파일명]
│   │   ├── [파일명]
│   │   ├── [파일명]
│   │   ├── [파일명]
│   │   ├── [파일명]
│   │   ├── [파일명]
│   │   └── [파일명]
│   ├── [하위 디렉토리]/                 # [레이아웃 컴포넌트]
│   │   ├── [파일명]                     # [네비게이션]
│   │   ├── [파일명]                     # [헤더]
│   │   └── [파일명]                     # [푸터]
│   ├── [카테고리]/                      # [관련 컴포넌트]
│   │   ├── [파일명]                     # [목록 컴포넌트]
│   │   ├── [파일명]                     # [카드 컴포넌트]
│   │   ├── [파일명]                     # [다이얼로그]
│   │   ├── [파일명]                     # [설정]
│   │   └── [파일명]                     # [관리]
│   ├── [카테고리]/                      # [관련 컴포넌트]
│   │   ├── [파일명]                     # [업로드]
│   │   ├── [파일명]                     # [목록]
│   │   ├── [파일명]                     # [카드]
│   │   ├── [파일명]                     # [뷰어]
│   │   ├── [파일명]                     # [표시]
│   │   └── [파일명]                     # [태그]
│   ├── [카테고리]/                      # [에디터 관련]
│   │   ├── [파일명]                     # [에디터]
│   │   ├── [파일명]                     # [툴바]
│   │   └── [파일명]                     # [메뉴 바]
│   ├── [카테고리]/                      # [검색 관련]
│   │   ├── [파일명]                     # [검색 입력]
│   │   ├── [파일명]                     # [결과 목록]
│   │   ├── [파일명]                     # [결과 아이템]
│   │   └── [파일명]                     # [필터]
│   └── [카테고리]/                      # [대시보드 관련]
│       ├── [파일명]                     # [통계 카드]
│       ├── [파일명]                     # [차트]
│       └── [파일명]                     # [최근 목록]
│
├── [디렉토리 4]/                        # [유틸리티 및 클라이언트]
│   ├── [서비스]/
│   │   ├── [파일명]                     # [브라우저용 클라이언트]
│   │   ├── [파일명]                     # [서버용 클라이언트]
│   │   ├── [파일명]                     # [재사용 가능한 쿼리]
│   │   └── [디렉토리]/                  # [마이그레이션]
│   ├── [서비스]/
│   │   ├── [파일명]                     # [클라이언트]
│   │   ├── [파일명]                     # [기능 함수]
│   │   ├── [파일명]                     # [기능 함수]
│   │   ├── [파일명]                     # [기능 함수]
│   │   └── [파일명]                     # [통합 처리]
│   ├── [디렉토리]/
│   │   ├── [파일명]                     # [파서]
│   │   ├── [파일명]                     # [파서]
│   │   ├── [파일명]                     # [파서]
│   │   ├── [파일명]                     # [파서]
│   │   └── [파일명]                     # [오케스트레이터]
│   ├── [서비스]/
│   │   └── [파일명]                     # [헬퍼]
│   ├── [서비스]/
│   │   └── [파일명]                     # [클라이언트]
│   ├── [파일명]                         # [공통 유틸리티]
│   └── [파일명]                         # [검증 스키마]
│
├── [디렉토리 5]/                        # [React Hooks]
│   ├── [파일명]                         # [상태 관리]
│   ├── [파일명]                         # [데이터 페칭]
│   ├── [파일명]                         # [상태 관리]
│   ├── [파일명]                         # [상태 관리]
│   └── [파일명]                         # [Debounce 훅]
│
├── [디렉토리 6]/                        # [Server Actions]
│   ├── [파일명]                         # [CRUD]
│   ├── [파일명]                         # [업로드/삭제]
│   ├── [파일명]                         # [검색]
│   ├── [파일명]                         # [관리]
│   └── [파일명]                         # [관리]
│
├── [디렉토리 7]/                        # [TypeScript 타입]
│   ├── [파일명]                         # [DB 타입]
│   ├── [파일명]                         # [클라이언트 타입]
│   ├── [파일명]                         # [응답 타입]
│   └── [파일명]                         # [공통 타입]
│
└── [디렉토리 8]/                        # [상수]
    ├── [파일명]                         # [타입 정보]
    ├── [파일명]                         # [플랜 정보]
    └── [파일명]                         # [역할 정보]
```

---

## 기술 스택 상세

### Frontend Stack

#### [프레임워크]
**버전**: [버전 번호]
**설정**:
```[언어]
// [설정 파일명]
/** @type {import('[타입]').Config} */
const config = {
  [옵션]: {
    [설정]: true,
  },
  [옵션]: {
    [설정]: ['[도메인]', '[도메인]'],
  },
}

module.exports = config
```

**최적화**:
- [최적화 전략 1]
- [최적화 전략 2]
- [최적화 전략 3]
- [최적화 전략 4]

---

#### [언어/프레임워크]
**설정**:
```json
// [설정 파일명]
{
  "compilerOptions": {
    "target": "[타겟]",
    "lib": ["[라이브러리]", "[라이브러리]", "[라이브러리]"],
    "allowJs": [true/false],
    "skipLibCheck": [true/false],
    "strict": [true/false],
    "noEmit": [true/false],
    "esModuleInterop": [true/false],
    "module": "[모듈]",
    "moduleResolution": "[해결 방식]",
    "resolveJsonModule": [true/false],
    "isolatedModules": [true/false],
    "jsx": "[jsx 모드]",
    "incremental": [true/false],
    "paths": {
      "@/*": ["[경로]"]
    },
    "plugins": [
      {
        "name": "[플러그인명]"
      }
    ]
  },
  "include": ["[파일 패턴]", "[파일 패턴]", "[파일 패턴]", "[파일 패턴]"],
  "exclude": ["[디렉토리]"]
}
```

---

#### [CSS 프레임워크]
**설정**:
```[언어]
// [설정 파일명]
import type { Config } from "[패키지명]"

const config: Config = {
  darkMode: ["[모드]"],
  content: [
    "[경로 패턴]",
    "[경로 패턴]",
    "[경로 패턴]",
  ],
  theme: {
    extend: {
      colors: {
        [컬러명]: "[값]",
        [컬러명]: "[값]",
        [컬러명]: "[값]",
        [컬러명]: "[값]",
        [컬러명]: "[값]",
        [카테고리]: {
          DEFAULT: "[값]",
          foreground: "[값]",
        },
        [카테고리]: {
          DEFAULT: "[값]",
          foreground: "[값]",
        },
        [카테고리]: {
          DEFAULT: "[값]",
          foreground: "[값]",
        },
        [카테고리]: {
          DEFAULT: "[값]",
          foreground: "[값]",
        },
        [카테고리]: {
          DEFAULT: "[값]",
          foreground: "[값]",
        },
        [카테고리]: {
          DEFAULT: "[값]",
          foreground: "[값]",
        },
        [카테고리]: {
          DEFAULT: "[값]",
          foreground: "[값]",
        },
      },
      borderRadius: {
        lg: "[값]",
        md: "[계산식]",
        sm: "[계산식]",
      },
    },
  },
  plugins: [require("[플러그인]")],
}

export default config
```

---

### Backend Stack

#### [클라이언트 라이브러리]
**설정**:
```[언어]
// [경로]/[파일명]
import { [함수명] } from '[패키지명]'
import { [타입명] } from '[경로]'

export function [함수명]() {
  return [함수명]<[타입]>(
    process.env.[환경변수]!,
    process.env.[환경변수]!
  )
}

// [경로]/[파일명]
import { [함수명], type [타입명] } from '[패키지명]'
import { [함수명] } from '[모듈]'
import { [타입명] } from '[경로]'

export function [함수명]() {
  const [변수명] = [함수명]()

  return [함수명]<[타입]>(
    process.env.[환경변수]!,
    process.env.[환경변수]!,
    {
      [옵션]: {
        get(name: string) {
          return [변수명].get(name)?.value
        },
        set(name: string, value: string, options: [타입명]) {
          try {
            [변수명].set({ name, value, ...options })
          } catch (error) {
            // [에러 처리 설명]
          }
        },
        remove(name: string, options: [타입명]) {
          try {
            [변수명].set({ name, value: '', ...options })
          } catch (error) {
            // [에러 처리 설명]
          }
        },
      },
    }
  )
}
```

---

#### [API 클라이언트]
```[언어]
// [경로]/[파일명]
import [클래스명] from '[패키지명]'

if (!process.env.[환경변수]) {
  throw new Error('[에러 메시지]')
}

export const [변수명] = new [클래스명]({
  apiKey: process.env.[환경변수],
})

// Rate Limiting
export class [클래스명] {
  private requests: number[] = []
  private readonly maxRequests: number
  private readonly timeWindow: number

  constructor(maxRequests: number, timeWindowMs: number) {
    this.maxRequests = maxRequests
    this.timeWindow = timeWindowMs
  }

  async acquire(): Promise<void> {
    const now = Date.now()
    this.requests = this.requests.filter(time => now - time < this.timeWindow)

    if (this.requests.length >= this.maxRequests) {
      const oldestRequest = this.requests[0]
      const waitTime = this.timeWindow - (now - oldestRequest)
      await new Promise(resolve => setTimeout(resolve, waitTime))
      return this.acquire()
    }

    this.requests.push(now)
  }
}

// [설명]: [제한 조건]
export const [변수명] = new [클래스명]([횟수], [시간])

// [설명]: [제한 조건]
export const [변수명] = new [클래스명]([횟수], [시간])
```

---

## 컴포넌트 설계

### 1. [컴포넌트 이름 1]

**목적**: [컴포넌트 목적 설명]
**타입**: [Client/Server] Component
**Props**:
```[언어]
interface [인터페이스명] {
  [프로퍼티]: [타입]
  [프로퍼티]?: ([파라미터]: [타입]) => void
  [프로퍼티]?: ([파라미터]: [타입]) => void
}
```

**상태 관리**:
```[언어]
const [[상태명], [세터명]] = useState<[타입] | null>(null)
const [[상태명], [세터명]] = useState(false)
const [[상태명], [세터명]] = useState(0)
const [[상태명], [세터명]] = useState<string | null>(null)
```

**주요 로직**:
```[언어]
const [함수명] = useCallback(([파라미터]: [타입]) => {
  if ([파라미터].length > 0) {
    const [변수명] = [파라미터][0]

    // Validate [항목]
    if (![상수명].includes([변수명].[속성])) {
      [세터명]('[에러 메시지]')
      return
    }

    // Validate [항목]
    const [변수명] = [함수명]([인자]) // [설명]
    if ([변수명].[속성] > [변수명]) {
      [세터명](`[에러 메시지 템플릿]`)
      return
    }

    [세터명]([변수명])
    [세터명](null)
  }
}, [[의존성]])

const [함수명] = async () => {
  if (![상태명]) return

  [세터명](true)
  [세터명](0)

  const [변수명] = new FormData()
  [변수명].append('[키]', [값])

  try {
    // Progress simulation
    const [변수명] = setInterval(() => {
      [세터명](prev => Math.min(prev + 10, 90))
    }, 200)

    const [변수명] = await [함수명]([인자], [인자])

    clearInterval([변수명])
    [세터명](100)

    [콜백]?.([변수명])

    // Reset
    setTimeout(() => {
      [세터명](null)
      [세터명](0)
      [세터명](false)
    }, 500)
  } catch (error) {
    [세터명](error.message)
    [세터명](false)
    [콜백]?.(error)
  }
}
```

**의존성**:
- `[라이브러리명]`
- `[함수명]` Server Action
- `[함수명]` 유틸리티

---

### 2. [컴포넌트 이름 2]

**목적**: [컴포넌트 목적 설명]
**타입**: [Client/Server] Component
**Props**:
```[언어]
interface [인터페이스명] {
  [프로퍼티]?: string
  [프로퍼티]?: (content: string) => void
  [프로퍼티]?: boolean
  [프로퍼티]?: string
}
```

**Extensions 설정**:
```[언어]
const extensions = [
  [확장명].configure({
    [옵션]: {
      [설정]: [1, 2, 3, 4],
    },
    [옵션]: {
      HTMLAttributes: {
        class: '[클래스명]',
      },
    },
  }),
  [확장명].configure({
    placeholder: [값] || '[기본 메시지]',
  }),
  [확장명].configure({
    openOnClick: false,
    HTMLAttributes: {
      class: '[클래스명]',
    },
  }),
  [확장명].configure({
    HTMLAttributes: {
      class: '[클래스명]',
    },
  }),
]
```

**자동 저장 로직**:
```[언어]
const [함수명] = useDebouncedCallback(
  async (content: string) => {
    try {
      await [함수명]([파라미터], { content })
      [세터명]('saved')
    } catch (error) {
      [세터명]('error')
    }
  },
  [시간] // [설명]
)

useEffect(() => {
  if ([변수명] && [변수명]) {
    [변수명].on('[이벤트]', ({ [파라미터] }) => {
      const html = [파라미터].getHTML()
      [함수명](html)
      [세터명]('saving')
      [함수명](html)
    })
  }
}, [[의존성], [의존성], [의존성]])
```

---

### 3. [컴포넌트 이름 3]

**목적**: [컴포넌트 목적 설명]
**타입**: [Client/Server] Component
**Props**:
```[언어]
interface [인터페이스명] {
  [프로퍼티]: string
  [프로퍼티]?: '[타입 1]' | '[타입 2]'
  [프로퍼티]?: (results: [타입][]) => void
}
```

**검색 로직**:
```[언어]
const [함수명] = useDebouncedCallback(
  async (query: string) => {
    if (!query.trim()) {
      [세터명]([])
      return
    }

    [세터명](true)
    [세터명](null)

    try {
      const data = [조건식]
        ? await [함수명]([인자], query)
        : await [함수명]([인자], query)

      [세터명](data)
      [콜백]?.(data)
    } catch (error) {
      [세터명](error.message)
    } finally {
      [세터명](false)
    }
  },
  [시간] // [설명]
)

useEffect(() => {
  [함수명]([인자])
}, [[의존성], [의존성]])
```

---

## 데이터 구조 및 알고리즘

### 1. [알고리즘 이름 1]

**목적**: [알고리즘 목적 설명]
**제약**: [제약 조건 설명]

**알고리즘**:
```[언어]
interface [인터페이스명] {
  [필드]: number
  [필드]: string
  [필드]: number
}

function [함수명](
  [파라미터]: string,
  [파라미터]: number = [기본값]
): [반환타입][] {
  const [변수명]: [타입][] = []

  // 1. [단계 설명]
  const [변수명] = [파라미터].split(/[정규표현식]/)

  let [변수명] = ''
  let [변수명] = 0
  let [변수명] = 0

  for (const [항목] of [변수명]) {
    const [변수명] = [함수명]([항목])

    // 2. [단계 설명]
    if ([변수명] + [변수명] <= [파라미터]) {
      [변수명] += ([변수명] ? '\n\n' : '') + [항목]
      [변수명] += [변수명]
    } else {
      // 3. [단계 설명]
      if ([변수명]) {
        [변수명].push({
          [필드]: [변수명]++,
          [필드]: [변수명],
          [필드]: [변수명],
        })
      }

      // 4. [단계 설명]
      [변수명] = [항목]
      [변수명] = [변수명]

      // 5. [조건 설명]
      if ([변수명] > [파라미터]) {
        const [변수명] = [항목].split(/[정규표현식]/)
        [변수명] = ''
        [변수명] = 0

        for (const [항목] of [변수명]) {
          const [변수명] = [함수명]([항목])

          if ([변수명] + [변수명] <= [파라미터]) {
            [변수명] += [항목] + '. '
            [변수명] += [변수명]
          } else {
            if ([변수명]) {
              [변수명].push({
                [필드]: [변수명]++,
                [필드]: [변수명].trim(),
                [필드]: [변수명],
              })
            }
            [변수명] = [항목] + '. '
            [변수명] = [변수명]
          }
        }
      }
    }
  }

  // 6. [단계 설명]
  if ([변수명]) {
    [변수명].push({
      [필드]: [변수명],
      [필드]: [변수명],
      [필드]: [변수명],
    })
  }

  return [변수명]
}

// [설명]
function [함수명](text: string): number {
  return Math.ceil(text.length / [값])
}
```

**시간 복잡도**: O(n), n = [설명]
**공간 복잡도**: O(n)

---

### 2. [알고리즘 이름 2]

**목적**: [알고리즘 목적 설명]

**알고리즘**:
```[언어]
function [함수명]([파라미터]: number[], [파라미터]: number[]): number {
  if ([파라미터].length !== [파라미터].length) {
    throw new Error('[에러 메시지]')
  }

  let [변수명] = 0
  let [변수명] = 0
  let [변수명] = 0

  for (let i = 0; i < [파라미터].length; i++) {
    [변수명] += [파라미터][i] * [파라미터][i]
    [변수명] += [파라미터][i] * [파라미터][i]
    [변수명] += [파라미터][i] * [파라미터][i]
  }

  [변수명] = Math.sqrt([변수명])
  [변수명] = Math.sqrt([변수명])

  if ([변수명] === 0 || [변수명] === 0) {
    return 0
  }

  return [변수명] / ([변수명] * [변수명])
}
```

**시간 복잡도**: O(d), d = [설명]
**공간 복잡도**: O(1)

---

### 3. [알고리즘 이름 3]

**목적**: [알고리즘 목적 설명]

**알고리즘**:
```[언어]
const [상수명] = new Set([
  '[MIME 타입]',
  '[MIME 타입]',
  '[MIME 타입]',
  '[MIME 타입]',
  '[MIME 타입]',
])

const [상수명]: Record<string, string[]> = {
  '[MIME 타입]': ['[확장자]'],
  '[MIME 타입]': ['[확장자]'],
  '[MIME 타입]': ['[확장자]'],
  '[MIME 타입]': ['[확장자]'],
  '[MIME 타입]': ['[확장자]'],
}

interface [인터페이스명] {
  valid: boolean
  error?: string
}

function [함수명](file: File, [파라미터]: [타입]): [반환타입] {
  // 1. [검증 단계 설명]
  if (![상수명].has(file.type)) {
    return {
      valid: false,
      error: `[에러 메시지]: ${file.type}`,
    }
  }

  // 2. [검증 단계 설명]
  const [변수명] = file.name.toLowerCase()
  const [변수명] = [상수명][file.type] || []
  const [변수명] = [변수명].some(ext =>
    [변수명].endsWith(ext)
  )

  if (![변수명]) {
    return {
      valid: false,
      error: `[에러 메시지]`,
    }
  }

  // 3. [검증 단계 설명]
  const [변수명] = [함수명]([파라미터])
  if (file.size > [변수명]) {
    return {
      valid: false,
      error: `[에러 메시지 템플릿]`,
    }
  }

  // 4. [검증 단계 설명]
  if (file.size === 0) {
    return {
      valid: false,
      error: '[에러 메시지]',
    }
  }

  return { valid: true }
}

function [함수명]([파라미터]: [타입]): number {
  switch ([파라미터]) {
    case '[옵션 1]':
      return [값] * 1024 * 1024 // [설명]
    case '[옵션 2]':
      return [값] * 1024 * 1024 // [설명]
    case '[옵션 3]':
      return [값] * 1024 * 1024 // [설명]
    default:
      return [값] * 1024 * 1024
  }
}
```

---

## 함수 시그니처

### Server Actions

#### [파일명]

```[언어]
/**
 * [함수 설명]
 * @param [파라미터] - [파라미터 설명]
 * @returns [반환값 설명]
 * @throws {Error} - [에러 조건 설명]
 */
export async function [함수명](
  [파라미터]: [타입]
): Promise<[반환타입]>

/**
 * [함수 설명]
 * @returns [반환값 설명]
 * @throws {Error} - [에러 조건 설명]
 */
export async function [함수명](): Promise<[반환타입][]>

/**
 * [함수 설명]
 * @param [파라미터] - [파라미터 설명]
 * @returns [반환값 설명]
 * @throws {Error} - [에러 조건 설명]
 */
export async function [함수명](
  [파라미터]: string
): Promise<[반환타입]>

/**
 * [함수 설명]
 * @param [파라미터] - [파라미터 설명]
 * @param [파라미터] - [파라미터 설명]
 * @returns [반환값 설명]
 * @throws {Error} - [에러 조건 설명]
 */
export async function [함수명](
  [파라미터]: string,
  [파라미터]: Partial<[타입]>
): Promise<[반환타입]>

/**
 * [함수 설명]
 * @param [파라미터] - [파라미터 설명]
 * @throws {Error} - [에러 조건 설명]
 */
export async function [함수명](
  [파라미터]: string
): Promise<void>
```

#### [파일명]

```[언어]
/**
 * [함수 설명]
 * @param [파라미터] - [파라미터 설명]
 * @param [파라미터] - [파라미터 설명]
 * @returns [반환값 설명]
 * @throws {Error} - [에러 조건 설명]
 */
export async function [함수명](
  [파라미터]: string,
  [파라미터]: [타입]
): Promise<[반환타입]>

/**
 * [함수 설명]
 * @param [파라미터] - [파라미터 설명]
 * @param [파라미터] - [옵션 설명]
 * @returns [반환값 설명]
 */
export async function [함수명](
  [파라미터]: string,
  [파라미터]?: {
    [옵션]?: '[값]' | '[값]' | '[값]'
    [옵션]?: '[값]' | '[값]'
    [옵션]?: string
    [옵션]?: number
    [옵션]?: number
  }
): Promise<[반환타입][]>

/**
 * [함수 설명]
 * @param [파라미터] - [파라미터 설명]
 * @returns [반환값 설명]
 * @throws {Error} - [에러 조건 설명]
 */
export async function [함수명](
  [파라미터]: string
): Promise<[반환타입]>

/**
 * [함수 설명]
 * @param [파라미터] - [파라미터 설명]
 * @throws {Error} - [에러 조건 설명]
 */
export async function [함수명](
  [파라미터]: string
): Promise<void>

/**
 * [함수 설명]
 * @param [파라미터] - [파라미터 설명]
 * @param [파라미터] - [파라미터 설명]
 * @returns [반환값 설명]
 */
export async function [함수명](
  [파라미터]: string,
  [파라미터]: string
): Promise<[반환타입]>
```

#### [파일명]

```[언어]
/**
 * [함수 설명]
 * @param [파라미터] - [파라미터 설명]
 * @param [파라미터] - [파라미터 설명]
 * @param [파라미터] - [파라미터 설명]
 * @returns [반환값 설명]
 */
export async function [함수명](
  [파라미터]: string,
  [파라미터]: string,
  [파라미터]?: number
): Promise<[반환타입][]>

/**
 * [함수 설명]
 * @param [파라미터] - [파라미터 설명]
 * @param [파라미터] - [파라미터 설명]
 * @param [파라미터] - [파라미터 설명]
 * @returns [반환값 설명]
 */
export async function [함수명](
  [파라미터]: string,
  [파라미터]: string,
  [파라미터]?: number
): Promise<[반환타입][]>

/**
 * [함수 설명]
 * @param [파라미터] - [파라미터 설명]
 * @param [파라미터] - [파라미터 설명]
 * @returns [반환값 설명]
 */
export async function [함수명](
  [파라미터]: string,
  [파라미터]?: number
): Promise<[반환타입][]>
```

---

### [API/서비스] 함수

```[언어]
/**
 * [함수 설명]
 * @param [파라미터] - [파라미터 설명]
 * @param [파라미터] - [파라미터 설명]
 * @returns [반환값 설명]
 * @throws {Error} - [에러 조건 설명]
 */
export async function [함수명](
  [파라미터]: string,
  [파라미터]: number
): Promise<string>

/**
 * [함수 설명]
 * @param [파라미터] - [파라미터 설명]
 * @param [파라미터] - [파라미터 설명]
 * @returns [반환값 설명]
 * @throws {Error} - [에러 조건 설명]
 */
export async function [함수명](
  [파라미터]: string,
  [파라미터]?: number
): Promise<string[]>

/**
 * [함수 설명]
 * @param [파라미터] - [파라미터 설명]
 * @returns [반환값 설명]
 * @throws {Error} - [에러 조건 설명]
 */
export async function [함수명](
  [파라미터]: string
): Promise<number[]>

/**
 * [함수 설명]
 * @param [파라미터] - [파라미터 설명]
 * @returns [반환값 설명]
 * @throws {Error} - [에러 조건 설명]
 */
export async function [함수명](
  [파라미터]: string
): Promise<{
  [필드]: string
  [필드]: string
  [필드]: string[]
  [필드]: number[]
}>
```

---

## 에러 처리 전략

### 에러 분류

```[언어]
// [경로]/[파일명]

export class [에러 클래스명] extends Error {
  constructor(
    message: string,
    public code: string,
    public statusCode: number = 500
  ) {
    super(message)
    this.name = '[에러명]'
  }
}

// [에러 카테고리]
export class [에러 클래스명] extends [부모 클래스] {
  constructor(message: string = '[기본 메시지]') {
    super(message, '[에러 코드]', [상태 코드])
  }
}

// [에러 카테고리]
export class [에러 클래스명] extends [부모 클래스] {
  constructor(message: string = '[기본 메시지]') {
    super(message, '[에러 코드]', [상태 코드])
  }
}

// [에러 카테고리]
export class [에러 클래스명] extends [부모 클래스] {
  constructor(resource: string) {
    super(`${resource}[메시지]`, '[에러 코드]', [상태 코드])
  }
}

// [에러 카테고리]
export class [에러 클래스명] extends [부모 클래스] {
  constructor(message: string, public fields?: Record<string, string>) {
    super(message, '[에러 코드]', [상태 코드])
  }
}

// [에러 카테고리]
export class [에러 클래스명] extends [부모 클래스] {
  constructor([파라미터]: string, message: string) {
    super(`${[파라미터]} [메시지]: ${message}`, '[에러 코드]', [상태 코드])
  }
}

// [에러 카테고리]
export class [에러 클래스명] extends [부모 클래스] {
  constructor(message: string) {
    super(message, '[에러 코드]', [상태 코드])
  }
}

// [에러 카테고리]
export class [에러 클래스명] extends [부모 클래스] {
  constructor(message: string) {
    super(`[메시지 접두사]: ${message}`, '[에러 코드]', [상태 코드])
  }
}
```

### 에러 핸들링 패턴

**Server Action에서**:
```[언어]
export async function [함수명](
  [파라미터]: string,
  [파라미터]: [타입]
) {
  try {
    const { userId } = await [함수명]()
    if (!userId) {
      throw new [에러 클래스명]()
    }

    const [변수명] = [파라미터].get('[키]') as [타입]
    if (![변수명]) {
      throw new [에러 클래스명]('[에러 메시지]')
    }

    // ... 처리 로직 ...

    return [변수명]
  } catch (error) {
    if (error instanceof [에러 클래스명]) {
      throw error // [에러 처리 설명]
    }

    // [에러 처리 설명]
    console.error('[로그 메시지]:', error)
    throw new [에러 클래스명](
      '[에러 메시지]',
      '[에러 코드]'
    )
  }
}
```

**클라이언트 컴포넌트에서**:
```[언어]
const [함수명] = async () => {
  try {
    await [함수명]([인자], [인자])
    [함수명].success('[성공 메시지]')
  } catch (error) {
    if (error instanceof [에러 클래스명]) {
      [함수명].error(error.message)
    } else if (error instanceof [에러 클래스명]) {
      [함수명].error('[에러 메시지]')
      [변수명].push('[경로]')
    } else if (error instanceof [에러 클래스명]) {
      [함수명].error(`[에러 메시지]: ${error.message}`)
    } else {
      [함수명].error('[기본 에러 메시지]')
    }
  }
}
```

### 재시도 로직

```[언어]
// [경로]/[파일명]

interface [인터페이스명] {
  maxAttempts: number
  delayMs: number
  backoff?: '[타입 1]' | '[타입 2]'
  onRetry?: (attempt: number, error: Error) => void
}

export async function [함수명]<T>(
  fn: () => Promise<T>,
  options: [타입명]
): Promise<T> {
  const {
    maxAttempts,
    delayMs,
    backoff = '[기본값]',
    onRetry,
  } = options

  let lastError: Error

  for (let attempt = 1; attempt <= maxAttempts; attempt++) {
    try {
      return await fn()
    } catch (error) {
      lastError = error as Error

      if (attempt === maxAttempts) {
        throw lastError
      }

      onRetry?.(attempt, lastError)

      const delay = backoff === '[옵션]'
        ? delayMs * Math.pow(2, attempt - 1)
        : delayMs * attempt

      await new Promise(resolve => setTimeout(resolve, delay))
    }
  }

  throw lastError!
}

// 사용 예시
export async function [함수명](
  [파라미터]: string,
  [파라미터]: number
): Promise<string> {
  return [함수명](
    async () => {
      const response = await [클라이언트].[메서드].[메서드]({
        [옵션]: '[모델명]',
        [옵션]: [/* ... */],
      })
      return response.[속성][0].[속성].[속성] || ''
    },
    {
      maxAttempts: [횟수],
      delayMs: [시간],
      backoff: '[타입]',
      onRetry: (attempt, error) => {
        console.warn(`[재시도 로그]`, error.message)
      },
    }
  )
}
```

---

## 캐싱 전략

### [상태 관리 라이브러리] 설정

```[언어]
// [경로]/[파일명]
'use client'

import { [클래스명], [프로바이더명] } from '[패키지명]'
import { [도구명] } from '[패키지명]'
import { useState } from 'react'

export function [컴포넌트명]({ children }: { children: React.Node }) {
  const [[변수명]] = useState(
    () =>
      new [클래스명]({
        defaultOptions: {
          queries: {
            staleTime: [시간], // [설명]
            cacheTime: [시간], // [설명]
            refetchOnWindowFocus: false,
            retry: [횟수],
          },
        },
      })
  )

  return (
    <[프로바이더명] client={[변수명]}>
      {children}
      <[도구명] initialIsOpen={false} />
    </[프로바이더명]>
  )
}
```

### 데이터 페칭 훅

```[언어]
// [경로]/[파일명]
import { [훅명], [훅명], [훅명] } from '[패키지명]'
import { [함수명], [함수명] } from '[경로]'

export function [훅명]([파라미터]: string) {
  return [훅명]({
    queryKey: ['[키]', [파라미터]],
    queryFn: () => [함수명]([파라미터]),
    staleTime: [시간], // [설명]
  })
}

export function [훅명]() {
  const [변수명] = [훅명]()

  return [훅명]({
    mutationFn: [함수명],
    onSuccess: (_, [파라미터]) => {
      // [캐시 무효화 설명]
      [변수명].invalidateQueries({ queryKey: ['[키]', [파라미터]] })
      // [캐시 무효화 설명]
      [변수명].invalidateQueries({ queryKey: ['[키]'] })
    },
  })
}
```

### [프레임워크] 캐싱

**[함수명] 사용**:
```[언어]
// Server Action에서 캐시 무효화
import { [함수명] } from '[모듈]'

export async function [함수명]([파라미터]: string, [파라미터]: [타입]) {
  // ... 처리 로직 ...

  // [캐시 무효화 설명]
  [함수명](`/${[파라미터]}`)

  return [변수명]
}
```

**fetch 캐싱**:
```[언어]
// 서버 컴포넌트에서
const [변수명] = await fetch(
  `${process.env.[환경변수]}/[엔드포인트]`,
  {
    next: {
      revalidate: [시간], // [설명]
      tags: ['[태그]'], // [설명]
    },
  }
).then(res => res.json())
```

---

## 보안 구현

### RLS (Row Level Security) 정책

**[리소스] 접근 제어**:
```sql
-- [정책 설명]
CREATE POLICY "[정책명]"
  ON [테이블명] FOR SELECT
  USING (
    id IN (
      SELECT [컬럼명]
      FROM [테이블명]
      WHERE [조건] = (auth.jwt() ->> '[키]')::text
    )
  );

-- [정책 설명]
CREATE POLICY "[정책명]"
  ON [테이블명] FOR UPDATE
  USING ([컬럼명] = (auth.jwt() ->> '[키]')::text)
  WITH CHECK ([컬럼명] = (auth.jwt() ->> '[키]')::text);
```

### XSS 방어

**입력 검증**:
```[언어]
import { z } from 'zod'

export const [스키마명] = z.object({
  [필드명]: z.instanceof(File)
    .refine(file => file.size <= [값] * 1024 * 1024, '[에러 메시지]')
    .refine(
      file => [상수명].has(file.type),
      '[에러 메시지]'
    ),
})

export const [스키마명] = z.object({
  [필드명]: z.string()
    .min([값], '[에러 메시지]')
    .max([값], '[에러 메시지]')
    .regex(/[정규표현식]/, '[에러 메시지]'),
})
```

**출력 이스케이프**:
```[언어]
// [라이브러리명] 사용
import [라이브러리명] from '[패키지명]'

function [컴포넌트명]({ html }: { html: string }) {
  const sanitized = [라이브러리명].sanitize(html, {
    ALLOWED_TAGS: ['[태그]', '[태그]', '[태그]', '[태그]', '[태그]', '[태그]', '[태그]', '[태그]'],
    ALLOWED_ATTR: ['[속성]'],
  })

  return <div dangerouslySetInnerHTML={{ __html: sanitized }} />
}
```

### CSRF 보호

**Server Actions 자동 보호**:
- [프레임워크] Server Actions는 자동으로 CSRF 토큰 검증
- origin 헤더 확인

### Rate Limiting

```[언어]
// [경로]/[파일명]
import { [클래스명] } from '[패키지명]'
import { [클래스명] } from '[패키지명]'

const [변수명] = new [클래스명]({
  url: process.env.[환경변수]!,
  token: process.env.[환경변수]!,
})

// [설명]
export const [변수명] = new [클래스명]({
  redis: [변수명],
  limiter: [클래스명].slidingWindow([횟수], '[시간 단위]'), // [제한 조건]
  analytics: true,
})

// [설명]
export const [변수명] = new [클래스명]({
  redis: [변수명],
  limiter: [클래스명].slidingWindow([횟수], '[시간 단위]'), // [제한 조건]
})

// Middleware에서 사용
export async function middleware(request: [타입]) {
  const ip = request.ip ?? '[기본값]'
  const { success } = await [변수명].limit(ip)

  if (!success) {
    return new [클래스명]('[에러 메시지]', { status: 429 })
  }

  return [클래스명].next()
}
```

---

## 성능 최적화

### 코드 스플리팅

```[언어]
// Dynamic Import
import [함수명] from '[모듈]'

const [컴포넌트명] = [함수명](() => import('[경로]'), {
  ssr: false,
  loading: () => <[컴포넌트명] />,
})
```

### 이미지 최적화

```[언어]
import [컴포넌트명] from '[모듈]'

<[컴포넌트명]
  src={[변수명]}
  alt="[설명]"
  width={[너비]}
  height={[높이]}
  className="[클래스명]"
  priority={false}
  loading="lazy"
/>
```

### Database 쿼리 최적화

**인덱스 활용**:
```sql
-- [테이블명] 테이블에 복합 인덱스
CREATE INDEX [인덱스명]
  ON [테이블명]([컬럼명], [컬럼명] DESC);

-- 검색용 [인덱스 타입] 인덱스
CREATE INDEX [인덱스명]
  ON [테이블명] USING [타입] ([함수]('[언어]', [컬럼명]));
```

**N+1 쿼리 방지**:
```[언어]
// ❌ 나쁜 예: N+1 쿼리
const [변수명] = await [클라이언트].from('[테이블명]').select('*')
for (const [항목] of [변수명]) {
  const [변수명] = await [클라이언트]
    .from('[테이블명]')
    .select('[컬럼명]')
    .eq('[컬럼명]', [항목].[컬럼명])
    .single()
}

// ✅ 좋은 예: JOIN
const [변수명] = await [클라이언트]
  .from('[테이블명]')
  .select(`
    *,
    [관계명]:[테이블명]([컬럼명])
  `)
```

---

## 테스트 전략

### Unit Tests ([테스팅 프레임워크])

```[언어]
// [경로]/[파일명]
import { describe, it, expect } from '[패키지명]'
import { [함수명] } from '../[파일명]'

describe('[테스트 대상]', () => {
  it('[테스트 케이스 설명]', async () => {
    const [변수명] = new File(['[내용]'], '[파일명]', {
      type: '[MIME 타입]',
    })

    const result = await [함수명]([변수명])

    expect(result).toContain('[예상 결과]')
  })

  it('[테스트 케이스 설명]', async () => {
    const [변수명] = new File(['[내용]'], '[파일명]', {
      type: '[MIME 타입]',
    })

    await expect([함수명]([변수명])).rejects.toThrow()
  })
})
```

### Integration Tests

```[언어]
// [경로]/[파일명]
import { describe, it, expect, beforeEach, afterEach } from '[패키지명]'
import { [함수명], [함수명] } from '../[파일명]'

describe('[테스트 대상]', () => {
  let [변수명]: string

  beforeEach(async () => {
    // [설정 설명]
    const [변수명] = await [함수명]()
    [변수명] = [변수명].id
  })

  afterEach(async () => {
    // [정리 설명]
    await [함수명]([변수명])
  })

  it('[테스트 케이스 설명]', async () => {
    const [변수명] = new FormData()
    [변수명].append('[키]', [함수명]())

    const [변수명] = await [함수명]([변수명], [변수명])

    expect([변수명].[속성]).toBeDefined()
    expect([변수명].[속성]).toBeDefined()
    expect([변수명].[속성]).toHaveLength([값])
  })
})
```

### E2E Tests ([E2E 프레임워크])

```[언어]
// [경로]/[파일명]
import { test, expect } from '[패키지명]'

test.describe('[테스트 스위트]', () => {
  test('[테스트 케이스 설명]', async ({ page }) => {
    await page.goto('/')
    await page.click('text=[텍스트]')

    // 로그인
    await page.fill('input[name=[이름]]', '[값]')
    await page.fill('input[name=[이름]]', '[값]')
    await page.click('button[type=submit]')

    // [단계 설명]
    await page.click('text=[텍스트]')

    // [단계 설명]
    await page.setInputFiles('input[type=file]', '[경로]')
    await page.click('text=[텍스트]')

    // [단계 설명]
    await expect(page.locator('text=[텍스트]')).toBeVisible()
    await expect(page.locator('text=[텍스트]')).not.toBeVisible({
      timeout: [시간],
    })

    // [검증 설명]
    await expect(page.locator('[선택자]')).toBeVisible()
  })
})
```

---

이 LLD 문서는 개발 중 참고하는 살아있는 문서로, 구현 과정에서 지속적으로 업데이트됩니다.
